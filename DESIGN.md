# DESIGN.md — Tally

> 動任何 UI 之前先讀這份。所有顏色、字級、間距一律引用下方 token，不硬編。

## 方向

溫暖、平靜的日常記錄工具。使用者一天開五次、每次三秒，所以**掃一眼看完**比資訊完整更重要。

**參照錨點**：使用者自用的 iOS app「小資算熱量」——暖米背景、低對比、圓潤。取其情緒核心，**資訊架構完全重做**（見下方「已否決的做法」）。

**色系來源**：沿用自家 Gambit 專案的 design system（`d:/Personal/Games/Gambit/design/gambit-design-system/colors_and_type.css`）的 cream + jade 子集。兩專案共用色系，對比度已在真實產品上驗證。棋盤色、地圖磚、金色高光不搬。

> Gambit 若改動色票，此處需同步。

## Tokens

### 色彩（只有淺色，不做暗色模式）

```css
:root {
  /* 表面 */
  --paper:        #FAF6F0;   /* 頁面底 · warm cream */
  --card:         #FCF9F3;   /* 卡片 / 分頁列 */
  --raised:       #F4EAD8;   /* 抬起面 */
  --rule:         #E0D3BD;   /* 分隔線 / 進度軌道 */
  --rule-subtle:  #ECE1CD;

  /* 文字 */
  --ink:          #3D2210;   /* 內文 · 對 paper 13.6:1 (AAA) */
  --ink-muted:    #7A5C44;   /* 次要 · 5.6:1 (AA) */
  --ink-faint:    #A88C76;   /* 裝飾 / placeholder · 永不用於內文 */

  /* 主色 · 深青瓷 jade */
  --accent:       #1C7059;   /* 進度填色 / CTA / 選中態 · 對 paper 5.55:1 */
  --accent-dark:  #155747;   /* hover / pressed */
  --accent-soft:  #CFE9E0;   /* 選取底色 / focus halo */
  --on-accent:    #FFFFFF;   /* accent 填色上的文字 · 4.5:1 */

  /* 破表警告 · 唯一的例外色 */
  --rule-field:   #9F8570;   /* 輸入框邊界 · 對 paper 3.22:1（過圖形 3:1） */

  --over:         #9A4330;   /* 文字 · 對 paper 6.06:1 */
  --over-fill:    #B8533A;   /* 進度填色 · 對 paper 4.50:1、對軌道 --rule 3.28:1（圖形需 3:1） */
  --over-stripe:  rgba(255,255,255,.22);  /* 斜紋疊層 · 疊在 --over-fill 上，非獨立色 */
}
```

**accent 用量守則**：jade 是常態基調（今天一切正常），`--over` 只在破表時出現。頁面上同時只該有一種**狀態**訊號色——**動作色不受此限**（CTA 與分頁選中態永遠是 jade，即使整頁在破表）。這個區分很重要：熱量沒破但脂肪先破，是每天都會出現的混合狀態，那時 jade 主數字與紅色脂肪條本來就該同框。

### 字體

```css
--font-body: -apple-system, "PingFang TC", "Noto Sans TC", "Microsoft JhengHei", sans-serif;
```

**全站只有這一支。** 零 webfont、零 CDN、零自架字體——iOS 拿到 SF Pro、Android 拿到 Noto Sans TC，都是平台自己為螢幕 UI 調校過的字。層次靠字重與字級做，不靠字型。

**所有數字用 `--font-body` ＋ `font-variant-numeric: tabular-nums`**。tabular 讓數字等寬，數值更新時不會左右跳動——這是 mono 唯一的實用價值，`tabular-nums` 就給得了，不需要換一支字體。

> 已實測：Windows（微軟正黑）tabular 生效。iOS 真機上線後補驗一次。

### 字階（1.25 major third，全頁六級以內）

```css
--t-xs:   0.72rem;   /* 11.5px · 標籤 */
--t-sm:   0.8rem;    /* 12.8px · 品項熱量、次要說明 */
--t-base: 0.875rem;  /* 14px   · 品項名、內文 */
--t-md:   1rem;      /* 16px   · 餐別名、CTA、日期 */
--t-hero: 2.75rem;   /* 44px   · 剩餘熱量主數字（600 weight） */
```

### tracking（字距隨字級走，不是全域一個值）

```css
--track-hero: -.022em;   /* 44px 主數字 · 大字放著不收會散 */
--track-md:   -.008em;   /* 16px 餐別名、CTA、頂欄 */
--track-base:  0;        /* 14px 內文 */
--track-xs:   .015em;    /* 11.5px 標籤與數值 · 小字放開才好讀 */
```

leading 反向：大字緊（主數字 `.95`、餐別名 `1.2`），內文鬆（`1.45`–`1.55`）。

### 間距（4pt）

```css
--s-1: 4px;  --s-2: 8px;  --s-3: 12px;
--s-4: 16px; --s-5: 20px; --s-6: 24px;
```

水平邊距固定 `--s-5`（20px）。

### 圓角與層次

```css
--r-btn:  16px;   /* 主 CTA、分頁列外框 */
--r-tab:  12px;   /* 分頁列內層 */
--r-pill: 999px;  /* 圓形節點、chip */
```

**不用陰影。** 分層靠 `--paper` / `--card` / `--raised` 的明度差。

## 元件規則

| 元件 | 規則 |
| --- | --- |
| 主 CTA「記一筆」 | `--accent` 填色、`--on-accent` 文字、min-height 52px、**固定在拇指區不隨內容捲動** |
| 次要動作 | 不填色，`--accent` 文字 |
| 熱量量尺 | 高 8px、圓角 4px、軌道 `--rule`、填色 `--accent` |
| 三大營養素條 | 常態高 3px，**破表時長到 6px**；三等分橫排，各自「標籤 → 條 → `60.5/126` 數值」由上而下。三條底部對齊 |
| 破表狀態 | 填色 `--over-fill` 打底、疊 -45° 斜紋（`--over-stripe` 2px / 透明 4px，週期 6px）、文字換 `--over`。熱量尺高度不變（8px） |
| 破表的非顏色訊號 | 紅綠色覺者不靠顏色也要分得出來，靠**兩層**：主訊號是營養素條的 3px → 6px 高度差（三條並排時直接分成兩類），紋理是第二層。紋理內部對比僅 1.47:1，**不可單獨承擔辨識**——所以 3px 常態高度上不塗紋，塗了也糊掉 |
| 三大營養素判定 | **各自獨立**。脂肪、碳水 >100% 轉破表；蛋白質不足**不**轉紅（吃不夠是待辦，不是錯誤） |
| 數對（`已吃/目標`） | 一律 `900/1860` 這種形式，**不加空格、不加「已吃」「目標」標籤**。前後做階層，否則兩個語意不同的數字長得一模一樣：當前值 `--ink` ＋ 600（會變動、要盯的）／斜線與目標值 `--ink-muted`（固定參考值）。破表時**只把當前值染 `--over`**——目標值本身沒有超標。**視覺階層對螢幕閱讀器無效**，容器要有 `aria-label="已攝取 903 大卡，目標 1860 大卡"`，內部 span 一律 `aria-hidden` |
| 品項份量 | `qty ≠ 1` 時在品名後顯示 `×2`（`--ink-muted`）；`qty = 1` 不顯示。份量不寫進食物名——記錯份量是最常見的錯誤，畫面上看不到就查不出來 |
| 時間軸節點 | 已吃＝實心 `--accent` 圓 9px；待記錄＝空心、1.5px `--ink-faint` 邊框（`--rule` 對 paper 只有 1.37:1，圖形元素需 3:1）。連接線 1px |
| 時間軸虛實線 | 線的虛實**一律看下端節點**：下端已記錄＝實線，下端待記錄＝虛線。中間餐次空著（例如沒吃早餐但有午餐）時這條規則才有唯一解 |
| 時間軸節點對齊 | 圓點中心對餐別標題的**文字中線**，不是行盒。已記錄 `margin-top: 5px`；**待記錄要 `17px`**——它包在 44px 觸控行裡、文字垂直置中，沿用 5px 會差 12px。改任何相關行高後要重量一次（用 Range 量文字墨水範圍，別目測） |
| 餐別標題 | `--t-md` / 700，下方一條 1px `--rule` 細線與品項切開 |
| 品項列 | 用 `ul`/`li`，名稱 `--t-base` / 400 `--ink`；熱量 `--t-sm` `--ink-muted` |
| 待記錄餐次 | 整行可點（真 `button`），直接進該餐別的記錄流程。chevron 用 `--ink-muted`——`--ink-faint` 對 paper 只有 2.92:1，而它是這一行唯一的「可點」視覺提示 |
| 分頁列 | 外框 `--r-btn`、底 `--card`、1px `--rule` 邊框；分頁 `--r-tab`，用真 `button` ＋ `aria-current="page"`。**選中態的辨識靠 jade 文字色（約 5:1）＋ 1px `--accent` inset 邊框**；`--raised` 底色只是輔助，它對 `--card` 實算僅 **1.14:1**，單獨用撐不起可辨識性 |
| 捲動範圍 | 只有時間軸的品項區捲。頂欄／額度／營養素／CTA／分頁列固定不動。捲軸隱藏（`scrollbar-width: none`），手機本就是 overlay scrollbar |
| 區塊分隔 | **不用 1px 硬線**。時間軸頂端用 10px 漸層遮罩（`-webkit-mask-image` ＋ `mask-image`，前綴不可省，舊 iOS Safari 會失效）。**底部刻意不做漸層**——捲到底時會把最後一項淡掉，而被切的文字本身就是「還有內容」的訊號 |
| 觸控區 | 一律 ≥44×44px。待記錄整行、分頁 44px；主 CTA 52px |
| 圖示 | 全部 inline SVG，`stroke-width: 1.6`、`fill: none`、`aria-hidden="true"`。**唯一例外：主 CTA 的加號用 2.2**——18px 圖示疊在 accent 填色上，1.6 撐不住。不用 emoji、不用 icon font |
| 進度條數值 | 寬度一律 `clamp(0, x, 100)%`——碳水 227.9/214 是 106.5%，不夾住會爆版。**分母為 0 或 NaN 時走降級畫面，不要把 NaN 送進 DOM** |
| 小數捨入 | **只在最終顯示時捨入，小計一律由未捨入值加總**。營養素顯示一位小數、熱量顯示整數；各自捨入再相加會讓餐別小計與主數字對不起來，天天看一定會發現 |
| 破表臨界 | `>` 才算破表，`==` 不算——「超出 0 卡」看起來像 bug。判定用**捨入後**的值，否則 1860.4 對 1860 會判成破表，但畫面上兩個數字長得一樣 |
| 主數字三數自洽 | 已吃、目標各自捨入後，剩餘由**捨入後的值相減**。直接捨入未捨入的差會讓 `903 + 957 ≠ 1861` 這種對不起來的組合出現在同一屏 |
| 載入態 | 主數字停在 `—`，時間軸顯示「載入中…」。**不得先渲染 0 或目標值再跳成真值**——「三秒瞄一眼」剛好會看到假數字。「讀到空」與「還沒讀到」必須在畫面上分得開 |
| 空狀態（每天早上） | **不做 0% 的最小可見態、不填那段空白**。數字（`1860`、`0/1860`）與四個空心節點已經讀得出「今天還沒開始」；補文案解釋已在下方否決，補假的進度條像素則是騙人 |
| 登入頁 | 置中：`--t-hero` / 600 / `--accent` 的 `Tally` ＋ 一行 `--ink-muted` 說明 ＋ 標準 CTA。**不放 Google 彩色 logo**——fill-based 圖示與「全部 inline SVG、`stroke-width: 1.6`、`fill: none`」的規則相斥，也省掉 Google 品牌指南的一串約束。hero 的 `line-height: .95` 會切住行盒，標題下方 margin 要比目測需要的再大一級 |
| 設定頁（第一版） | 唯讀 `dl` 清單：今日目標四項 ＋「怎麼算出來的」（體重／量測日／身高／年齡／BMR／活動係數／TDEE／目標／三大比例）＋ 兩個次要動作（記體重、編輯身體參數）＋ 登出。**目標怎麼來的要看得到**——1860 是算出來的，看不到算式就只能相信它 |
| 設定頁的編輯與記體重 | 都走**同一套 sheet**，不另做頁面。身體參數：生日／身高＋性別／目標＋活動係數／三大比例三欄，欄位規則同新增食物（floating label）。三大比例相加必須 100，前端先擋（DB 的 check 是最後一道）。記體重：量測日預設今天、體重必填、體脂選填；同一天再記是**覆蓋**（`unique(user_id, measured_on)`，寫入帶 `Prefer: resolution=merge-duplicates`）。兩者存檔後重算目標並**留在設定頁**，不跳回今日頁 |
| floating label 的高度 | 欄位 60px、上內距 26px、label 浮起後 `top: 8px` ＋ `line-height: 1`。**這四個值是一組的**：樣張原本的 52/18/13 會讓 label 行盒與輸入文字重疊（實測重疊 4.7px），字擠成一團。改動任一個要重量 label 底緣與文字起點的距離（目前 7.3px） |
| 記一筆 sheet | 走**同頁覆蓋層**（sheet），不做獨立頁——獨立頁等於整頁重載加重打 API，還得為今日頁做本地快取。清單這一屏**沒有標題文字**：chip 列的選中態已經說了是哪一餐，再寫一次是同一個資訊講兩遍；關閉鈕併進 chip 列同一行，省下的 52px 在 745px 的畫面裡是多看到一整列食物。容器用 `aria-label` 提供無障礙名稱（`role="dialog" aria-label="早餐"`）。**新增食物／記體重／身體參數三張表單相反，要顯示標題**——它們沒有 chip 給情境線索，拿掉就不知道自己在哪 |
| sheet 版面分區 | 固定不動、由上而下：餐別 chip 列、搜尋框。**唯一捲動區**：已選段落（有選取才出現）→ 該餐別常吃 → 全部食物。底部固定：確認列（有選取才出現）。**sheet 只負責新增，不負責刪除**（刪除入口見下方「今日頁品項刪除」） |
| 記一筆入口 | 兩個：今日頁主 CTA「記一筆」（餐別未定，chip 依時間預選）；時間軸的餐別標題列或待記錄行（餐別已定，直接開對應餐別的 sheet）。主 CTA 文案維持「記一筆」 |
| 食物清單多選 | **可多選**——使用者平日早餐固定三樣，逐一新增要走三趟流程。未選取列右側顯示單份熱量；選取後同一位置換成加減按鈕組（減號、數字、加號），不再顯示熱量 |
| 加減按鈕組 | 加減鈕各自 ≥44×44px；**數字本身可點**，點了叫數字鍵盤填精確值（份量可小數）；整組寬度上限 130px |
| 選取態視覺訊號 | **不鋪底色**。訊號是勾選圖示變實心＋右側長出加減按鈕組——加減鈕組出現本身就是最強的差異，三列連排鋪 `--accent-soft` 會與 cream 背景形成大色塊（見下方「已否決的做法」） |
| 已選段落 | 在捲動區**最上方獨立成一段**，不隨餐別切換而消失，可直接就地改份量或取消。被選取的項目從常吃或全部食物清單**移除，不重複出現**——同一項目在一屏出現兩個可互動實例是同步與混淆風險 |
| 切換餐別（已選） | 不清空已選項目，已選跟著搬到新餐別 |
| 底部確認列 | 小計靠左（例如 `3 樣 · 363 卡`）、加入後剩餘靠右（`剩 594`），中間留白分開；下方整條加入按鈕。**不加「已選」「還能吃」這類標籤字**——數字自己會說話。數對階層照既有規則：小計 `--ink` ＋ 600（會變動、要盯的），剩餘 `--ink-muted`（固定參考值） |
| 加入成功 | sheet 自動關閉回今日頁，新品項在時間軸上以 `--accent-soft` 短暫高亮後淡出，只動 `opacity` 與 `transform`，遵守 `prefers-reduced-motion` |
| 寫入失敗（記一筆） | 錯誤訊息**就地可見**，已選內容不清空，按鈕換成「重試」。不做離線佇列（YAGNI）——失敗要當場看得見即可 |
| 搜尋比對範圍 | 同時比對**品名與店家**——使用者有五筆「雞胸餐盒」只靠店家區分，品名裡的店家標記與 vendor 欄格式不一致，只比對品名找不到 |
| 搜尋框 | 是**模式切換器**：沒輸入時清單顯示該餐別常吃接全部食物；一有輸入，搜尋結果取代整段清單，兩段標題都不出現。同時只存在一段清單 |
| 新增食物入口 | 搜尋框只要有輸入，結果清單末尾永遠有一列「新增『輸入的字』到食品庫」；零結果時它是唯一一列。**不能只在零結果時才出現**——搜到部分結果時使用者一樣可能需要新增 |
| 切換餐別（搜尋） | 不重置搜尋關鍵字 |
| 新增食物表單 label | **floating label**：label 是真的 `label` 元素，初始在框內看起來像 placeholder，聚焦或有值時縮小上移到框線上。純 CSS 用 `:placeholder-shown` 與 `:focus` 加 `transform`，不寫 JS。**禁止用 placeholder 冒充 label**——一輸入就消失、對比度不足、螢幕閱讀器支援不一致，是 WCAG 3.3.2 常見失敗案例 |
| 新增食物表單欄位 | 三行：品名與店家並排、熱量整行、蛋白質脂肪碳水三欄並排（順序固定）。**沒有分類欄，沒有份量單位欄**。必填只有品名與熱量；**三大營養素留空當 0**——無糖飲料的蛋白質與脂肪本來就是 0，不該逼人打三個零。熱量不給這個待遇：它是這裡唯一的核心數字，忘了填那筆等於沒記 |
| 食物清單列顯示 | 品名為主，店家在下方第二行用 `--ink-muted`；沒有店家就沒有第二行。**店家那一行不可截斷或省略**——它是區分同名品項的唯一資訊 |
| 今日頁品項刪除入口 | 刪除在**今日頁，不在 sheet**——看到錯的那筆就在原地刪，不必先想它屬於哪一餐 |
| 今日頁品項刪除手勢 | 左滑露出刪除鈕，用 **CSS `scroll-snap`** 實作（品項列是橫向 scroll container，品項內容與刪除鈕兩個 snap 點），不寫觸控事件 JS |
| 刪除的鍵盤路徑 | 手勢對鍵盤與螢幕閱讀器是死路，所以品項內容本身是原生 `button`，點擊它也會露出刪除鈕。左滑與點擊兩條路都通 |
| 刪除鈕樣式 | 垃圾桶 inline SVG，不放文字；紅底 `--over-fill`、白色圖示、寬 56px、高度撐滿該列且不小於 44px，有 `aria-label` |
| 日期切換 | 今日頁可切換日期，補記過去的日子。日期文字本身是原生 `input type="date"`，點了叫系統日曆；旁邊一顆左箭頭回前一天——原生元件零成本，補記三天前不用按三次 |
| 日期右箭頭 | 在今天要**停用**（不能看未來）。日期文字可點回今天 |
| 歷史日記一筆 | 在歷史日按「記一筆」，記到當前檢視的那一天 |
| 歷史日主數字 | 改顯示**攝取量**（例如 `1266`），下方數對 `1266/1860`，標籤從「還能吃」換成「攝取」。版面與今天完全一樣，只換數字與標籤——回頭看過去問的是「那天吃了多少」，不是「還能吃多少」 |
| 「今天」的定義 | 就是系統日期，**不做凌晨分界**。跨午夜的宵夜由使用者自己切到前一天補登 |
| 熱量單位 | 全站用中文「**卡**」，不用 kcal。畫面顯示「卡」，`aria-label` 寫「大卡」 |
| 剩餘／超出的符號 | **不用正負號**（`+594` 會被讀成「多攝取 594」）。維持「剩 594」與「超出 193」兩種寫法 |

## 版面高度（實測，別用螢幕高度設計）

「一屏」指的是**瀏覽器可視高度**，不是裝置螢幕高度。iPhone 14 Pro 螢幕 844px，但 Safari 有網址列＋工具列時只剩約 **745px**。

- 實作用 `100dvh`，**不要用 `100vh`**（iOS 會被工具列吃掉）
- 底部固定區加 `padding-bottom: env(safe-area-inset-bottom)`，並在 viewport meta 加 `viewport-fit=cover`，否則 home indicator 會壓住分頁列
- 設計基準＝ **745px**。實測：四餐全記完的破表屏在此高度溢出約 96px，**這是預期的**——那時使用者已經吃完，時間軸是回顧性質，捲動可接受
- 但常態屏（有待記錄餐次）在 iPhone SE 這類小螢幕會把待記錄行推出視野。功能不會斷（CTA 是通用入口），只是少一步捷徑

## 語意與鍵盤（實作不可省，樣張已示範）

顏色字級間距寫得再細，這一節缺了就會做出鍵盤用不了的頁面。

- `<html lang="zh-Hant">`，否則 VoiceOver 會用系統語言唸中文
- **可點的東西一律用原生 `button`**，不要 `div` ＋ `cursor: pointer`——後者鍵盤 Tab 跳不到、螢幕閱讀器讀不出角色，且 iOS Safari 對非互動元素的 `:active` 不保證觸發
- 每個可聚焦元素都要有 `:focus-visible`
- 圖示按鈕要有 `aria-label`；純裝飾 SVG 一律 `aria-hidden="true"`
- 清單用 `ul`/`li`，讓 rotor 能逐項跳
- landmark：`header` / `main` / `nav`，頁面要有一個 `h1`
- 純視覺的進度條 `aria-hidden`（數值已由旁邊的數對文字提供，不必讀兩次）
- 主數字接上資料後加 `aria-live="polite"`，讓記錄完成有聽得到的確認
- `:hover` 包進 `@media (hover: hover)`——iOS 有 tap 後 hover 卡住的已知行為，會讓元素看起來像被 disable
- 重置樣式的 `button` 要補 `-webkit-appearance: none`

## 互動與動效

- 只動 `opacity` 與 `transform`，不動 layout 屬性
- `160ms cubic-bezier(0.4, 0, 0.2, 1)`——這是**按鈕級**規格（狀態變化、透明度）
- **sheet 進出場是唯一的例外**：`280ms cubic-bezier(0.32, 0.72, 0, 1)` 進場、`200ms` 同曲線退場。
  按鈕級的 160ms 套在 650px 的位移上會像被彈出來；抽屜類元件需要更長的時間與更早煞車的曲線。
  進場動畫只在**真正開啟那一次**掛上，切餐別／切 view／載入完成一律走增量更新不重建外殼——
  外殼一重建動畫就從頭再播一次。退場要等 `animationend` 才清 DOM，並留一個計時器 fallback
- CTA 按下 `translateY(1px)`
- **每個可點元素都要有 `:active`，不能只有 `:hover`**——手機沒有 hover，只寫 hover 等於按下去毫無回饋。待記錄行 `.5`、分頁 `.6`、日期箭頭 `.5`。回饋在按下的當下發生，不等放開
- `:focus-visible` 2px `--accent` 外框、offset 3px，**即時出現不做過場**
- `prefers-reduced-motion: reduce` → 全部降到 `0.01ms`，CTA 位移取消
- **破表狀態切換不做過場**：3px → 6px 是 layout 屬性，直接跳，不補 transition

## 禁止事項

本專案的黑名單，每一條都有踩過的理由：

- **不用環圈進度。** 圓環從 Apple Watch 三環來，那裡「填滿＝達成＝好」；吃東西相反，吃到目標不是成就、吃爆才是問題。用橫向量尺。
- **不做暗色模式。**（2026-07-27 決定，只維護淺色一套）
- **不做綠色「達標」狀態。** 綠色若代表「在範圍內」，一天中多數時間都會亮著，常態的顏色沒有資訊量。且使用者 Notion 六天資料裡蛋白質從未達標，那會是死功能。
- **不替使用者預先分配剩餘額度。** 曾把剩餘 957 切成「晚餐 670 / 點心 287」，結果使用者得在心裡反推「不吃點心就能吃 957」——發明一個規則再要人反推它，是設計失敗。改為顯示共用總額。
- **CJK 不用 italic。** 強調靠字重與 `--accent`。
- **不從第三方主機載入資源**（webfont、CDN 的 CSS／JS、外連圖）。webfont 會讓首屏閃字（FOUT）並多一次 DNS＋TLS 往返，而這個 app 的情境是一天開五次、每次三秒；CDN 函式庫則是拿可用性與隱私換一點方便。**資料 API 不在此列——Supabase 就是後端。** 此條 v1.5 前寫作「不用任何外部請求」，字面上把後端一起擋掉了；「該不該多一個依賴」是另一個問題，各自論證，別假借這條。
- **不用陰影分層。**
- **不硬編色值、字級、間距。** 一律 `var(--token)`；需要新值先加 token。
- **不照抄錨點。** 「小資算熱量」是情緒參照，不是版型範本。

## 已否決的做法（避免重蹈）

| 做法 | 否決理由 |
| --- | --- |
| 沿用錨點的卡片堆疊版型 | 環圈佔滿第一屏只傳達一個數字；最高頻動作「加入食品」藏在捲動後面；空狀態各佔一整張卡 |
| 環圈當主視覺 | 見上方禁止事項 |
| 把「已攝取」放在主數字位置 | 打開 app 要問的是「還能吃多少」，不是「吃了多少」 |
| 飲料點心併進正餐 | 使用者六天每天都有一筆點心，是固定習慣；併入就再也看不出點心吃掉多少——那正是減脂最該單獨盯的一類 |
| 用 11px 的「!」小圖示標示破表 | 實測太小，看起來像引號。改用斜紋 |
| 斜紋用實色 + 亮條（`#B8533A` 4px / `#C06A50` 4px） | 在 8px 熱量尺上像斑馬線，跟 cream 的溫度打架；更嚴重的是 3px 營養素條上完全糊成純紅，「不靠顏色也能辨識」等於只在一半的元件上成立 |
| 目標刻度線 + 只有溢出段帶紋（表示超出比例） | 旁邊 `超出 229 卡` 已經把量講完，條上再編碼一次是重複，換來的是兩段寬度與刻度線定位的實作成本 |
| 靠垂直細紋（90°）撐住 3px 條 | 垂直線確實不受條高影響，但 3px 上只是「勉強看得到」，撐不起無障礙底線。改用高度差當主訊號 |
| 自架 subset 數字字體 | 2026-07-28 比過六種＋一種混用（Inter / IBM Plex Mono / Newsreader / Cubic 11 / Cubic 11+Inter）。自架換得到的是**跨平台一致的數字長相**——系統字在 iOS 是 SF Mono、Windows 是 Consolas、Android 是 Roboto Mono，同一個 app 三種樣子；檔案成本也極低（subset 到 `0-9 . /` 後 1.3–4.8KB）。否決理由：這個一致性換不到足夠價值，系統字在各平台拿到的都是該平台調校過的最佳解，而多一支字體就多一層要維護的東西 |
| Cubic 11（點陣字，Gambit 同款） | 除上述理由外另有硬傷：點陣字只在設計尺寸的整數倍銳利（11 / 22 / 33 / 44px），Tally 的 14px 餐別熱量與 11.5px 營養素都對不上，且 14px 卡在 11 與 22 中間兩邊不合用——要採用就得推翻整個 1.25 字階。點陣字也沒有真正的粗體，撐不起 600 weight 的主數字 |
| 數字用 mono | 縱向對齊靠 `tabular-nums` 就成立，mono 額外帶來的只有等寬字元的工程感，跟「溫暖平靜」的方向打架。Apple 自己也是 iOS 數字用 SF Pro 的 tabular figures，只有程式碼情境才用 SF Mono |
| 用一行字解釋剩餘額度是共用的（「接下來這兩餐還有 957 卡可以分」） | 主數字本身就是 957，時間軸下方就列著兩個待記錄餐次——共用是讀得出來的，不必再講一次。需要文字去解釋的介面，代表 mapping 本身弱 |
| 右上角寫成「已吃 903／目標 1860」兩行帶標籤 | 佔兩行、加兩個詞，換來的資訊等於 `903/1860` 加階層就能給。標籤是給第一次看的人，天天用的人只需要數字 |
| 分步 sheet（選餐別 → 選食物 → 填份量三屏推進） | 塞不進已記錄品項，且常吃一鍵變三步 |
| 食品庫用店家分兩層 | 23 筆裡 vendor 分散在 11 個店家、8 個只有一筆，群組數接近項目數，分層沒有換到查找效率 |
| 每列顯示份量 × 單價的即時小計 | 底部已有總計，記錄當下沒人算單品 |
| 刪除用底部動作選單 | 左滑露出再點刪除本來就是兩個動作，彈窗只是多包一層卻沒換到安全性 |
| 選取列鋪 `--accent-soft` 底色 | 三列連排形成大色塊，與 cream 背景打架；加減按鈕組出現本身已是最強訊號 |
| 把欄位名融進 placeholder | 一輸入就消失、對比度不足、螢幕閱讀器支援不一致，是 WCAG 3.3.2 常見失敗案例 |
| 今日頁每個餐別直接放常吃 chip | 四排 chip 約 144px，而 745px 基準下破表屏已溢出 96px，會毀掉一屏不捲 |

## 尚未設計

- 破表且仍有待記錄餐次時的主數字文案（「超出 229」對還沒吃晚餐的人資訊不足）
- 剩餘額度的分配比例規則（第一版不做，只顯示共用總額）
- 今日頁品項列要不要顯示店家：三個同名「雞胸餐盒」在今日頁看起來一模一樣（sheet 的清單已分兩行解決）

## 版本紀錄

- **v1 · 2026-07-27** — 走完設計流程 ⓪→➊→➋。骨架＝時間軸＋共用額度＋一屏不捲＋CTA 拇指區；色系＝Gambit cream + jade（配色樣張比過 5 欄，cream+jade 勝出）。
- **v1.1 · 2026-07-28** — 破表狀態細琢。斜紋改疊白細紋（原實色亮條在 cream 上太吵）；補上 v1 沒驗證的漏洞——3px 營養素條上斜紋會糊掉，改由破表加高至 6px 當主訊號。另補元件規則兩列：分頁列選中態、捲動範圍。
- **v1.2 · 2026-07-28** — 字體定案：**取消 `--font-mono`**，數字改用 `--font-body` ＋ `tabular-nums`，全站單一字體。比過六種自架 subset 方案後否決自架（理由見「已否決的做法」）。
- **v1.3 · 2026-07-28** — 走 apple-design 精煉。補 tracking／leading 分級；砍掉共用額度的解釋文字與右上角標籤，改 `903/1860` ＋前後階層；`:active` 補齊（原本只有 hover，手機按下去沒回饋）；1px 硬分隔線改捲動邊緣漸層；修正待記錄節點圓點錯位 12.1px（實測，非目測）。
- **v1.4 · 2026-07-28** — 三路獨立評審（a11y／UX／視覺）後的修補，共 60 條 findings。重點：**「一屏」的定義從 844 螢幕高改為 745 可視高**（原本整個版面建在真機不存在的高度上）；新增「語意與鍵盤」整節（原本規範完全沒有這層，是 `div` 當按鈕、無 landmark、清單不用 `ul` 的根因）；修正 v1.1 對分頁選中態的錯誤宣稱（`--raised` 對 `--card` 實算 1.14:1，撐不起辨識，改由 jade 文字＋accent 邊框承擔）；訊號色規則改為區分「狀態色」與「動作色」；補份量標記、虛實線端點規則、clamp／NaN、捨入規則。
- **v1.5 · 2026-07-28** — 今日頁實作落地（`index.html` / `app.css` / `app.js`），版型照 `mockup.html` 逐項對過。補七列元件規則：破表臨界（`>`）、主數字三數自洽的捨入順序、載入態、空狀態（決策為「什麼都不加」）、登入頁、第一版設定頁。登入頁與設定頁原列「尚未設計」，因規則已能唯一推導（token、CTA、字階全定死），未另走決策樣張。
- **v1.6 · 2026-07-28** — 修正禁止事項「不用任何外部請求」的表述。原句字面上把 Supabase 一起擋掉了（它是後端，一定要打），收窄為「不從第三方主機載入資源」。實質擋住的東西不變（webfont、CDN 函式庫），但不再被拿來當「不加依賴」的理由——那是獨立的判斷。連帶更正 `CLAUDE.md` 同一句。
- **v1.7 · 2026-07-28** — 「記一筆」流程定案（`sample-log-entry.html` 7 屏）：sheet 走同頁覆蓋層、可多選＋加減按鈕組取代熱量欄、已選段落固定在捲動區頂端且跨餐別保留；搜尋同時比對品名與店家，新增食物入口常駐清單末尾，表單改 floating label；刪除入口移回今日頁，左滑用 CSS `scroll-snap` 實作，品項本身仍是可點 `button` 保留鍵盤路徑；第一版納入日期切換（`input type="date"` ＋ 左箭頭，歷史日主數字改顯示攝取量）。熱量單位全站定案為「卡」。「記一筆」流程自「尚未設計」移除，改列設定頁編輯版範圍已定。
- **v1.8 · 2026-07-28** — 第一版功能全部落地：「記一筆」sheet、今日頁刪除、日期切換、設定頁編輯＋記體重。實作中修掉四件樣張沒答的事：floating label 的 52/18/13 會讓標籤與輸入文字重疊（改 60/26/8＋`line-height: 1`）；品項寬度是分數值（320.4px）導致刪除鈕沿右緣露出一條紅線（`margin-left: 1px` 推出裁切外）；底部確認列的 `.line` 與時間軸連接線的 `.line` 撞名被壓成直排（改 `.pick-line`）；在設定頁存檔會被丟回今日頁（頂欄與分頁改由 `renderHeader` 統一渲染、`state.tab` 記住分頁）。另補三條元件規則：設定頁的編輯與記體重、floating label 高度、設定頁第一版內容更新。
- **v1.9 · 2026-07-28** — 走完設計流程 ➍（四路並行：`hallmark` 快篩、`web-design-guidelines` 合規、`impeccable` 深評、`emil-design-eng` polish 審表），19 條 findings 全數處理。**兩個 P0 都是「看起來成功、其實沒有」類**：`db()` 沒有逾時，收訊差時 fetch 永遠不 reject，按鈕停在「加入中…」而使用者會以為記好了（補 `AbortSignal.timeout(8000)` ＋中文錯誤文案）；載入失敗後點設定會進到空殼分頁且回不去重試（補 `state.failed`，失敗期間留在提示畫面）。修掉兩條違反本檔硬約束的：品項列沒有 `:active`（全站唯一零回饋的可點元素）、floating label 過場動 `top`／`font-size` 兩個 layout 屬性（改 `translateY` ＋ `scale(.82)`，`.82 × --t-base` 正好是 `--t-xs`）。無障礙實算修正三處：未勾選圈 2.99:1、floating label 與 placeholder 2.92:1、輸入框邊框 1.37:1（新增 `--rule-field` token）。「觸控區 ≥44」與「加減組上限 130px」兩條規則原本互不相容，把鈕間距收成 0 後 44+42+44 剛好同時成立。另補：今日頁**同名品項才顯示店家**（三筆「雞胸餐盒」在今日頁本來完全一樣）、滑開刪除時品名用 `position: sticky` 釘在左緣（原本被推出視野，等於在看不到刪什麼的情況下按刪除）、常吃記住上次份量、歷史日的確認列不再寫「剩 N」。
- **v1.10 · 2026-07-28** — 使用者對 v1.9 的兩處裁決。**清單屏拿掉標題文字**（chip 選中態已經說了是哪一餐），關閉鈕併進 chip 列同一行——只拿掉文字省不到高度（那一行是 44px 關閉鈕撐的），要連鈕一起移才真的收回 52px；三張表單相反，保留標題。**三大營養素改選填、留空當 0**——原本判定「不改」是把場景套錯了：無糖飲料的蛋白質脂肪是**真的 0**，不是「查不到」，`not null` 的 schema 也給不出第三態，逼人打零沒有換到任何準確度。熱量維持必填。
- **樣張現況** — 過程樣張（`design-sample-v2` / `palette-options` / `over-stripe-options` / `font-options`）決策已全數固化進本檔，2026-07-28 移除。今日頁樣張＝**`mockup.html`**；「記一筆」流程樣張＝**`sample-log-entry.html`**，實作以兩者為準。
