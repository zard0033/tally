# DESIGN.md — Tally

> 動任何 UI 之前先讀這份。所有顏色、字級、間距一律引用下方 token，不硬編。

## 方向

溫暖、平靜的日常記錄工具。使用者一天開五次、每次三秒，所以**掃一眼看完**比資訊完整更重要。

**參照錨點**：使用者自用的 iOS app「小資算熱量」——暖米背景、低對比、圓潤。取其情緒核心，**資訊架構完全重做**（見下方「已否決的做法」）。

**色系來源**：沿用自家 Gambit 專案的 design system（`d:/Personal/Games/Gambit/design/gambit-design-system/colors_and_type.css`）的 cream + jade 子集。兩專案共用色系，對比度已在真實產品上驗證。棋盤色、地圖磚、金色高光不搬。

> Gambit 若改動色票，此處需同步。

## Tokens

### 色彩（只有淺色，不做暗色模式）

```css
:root {
  /* 表面 */
  --paper:        #FAF6F0;   /* 頁面底 · warm cream */
  --card:         #FCF9F3;   /* 卡片 / 分頁列 */
  --raised:       #F4EAD8;   /* 抬起面 */
  --rule:         #E0D3BD;   /* 分隔線 / 進度軌道 */
  --rule-subtle:  #ECE1CD;

  /* 文字 */
  --ink:          #3D2210;   /* 內文 · 對 paper 13.6:1 (AAA) */
  --ink-muted:    #7A5C44;   /* 次要 · 5.6:1 (AA) */
  --ink-faint:    #A88C76;   /* 裝飾 / placeholder · 永不用於內文 */

  /* 主色 · 深青瓷 jade */
  --accent:       #1C7059;   /* 進度填色 / CTA / 選中態 · 對 paper 5.55:1 */
  --accent-dark:  #155747;   /* hover / pressed */
  --accent-soft:  #CFE9E0;   /* 選取底色 / focus halo */
  --on-accent:    #FFFFFF;   /* accent 填色上的文字 · 4.5:1 */

  /* 破表警告 · 唯一的例外色 */
  --rule-field:   #9F8570;   /* 輸入框邊界 · 對 paper 3.22:1（過圖形 3:1） */

  --over:         #9A4330;   /* 文字 · 對 paper 6.06:1 */
  --over-fill:    #B8533A;   /* 進度填色 · 對 paper 4.50:1、對軌道 --rule 3.28:1（圖形需 3:1） */
  --over-stripe:  rgba(255,255,255,.22);  /* 斜紋疊層 · 疊在 --over-fill 上，非獨立色 */
}
```

**accent 用量守則**：jade 是常態基調（今天一切正常），`--over` 只在破表時出現。頁面上同時只該有一種**狀態**訊號色——**動作色不受此限**（CTA 與分頁選中態永遠是 jade，即使整頁在破表）。這個區分很重要：熱量沒破但脂肪先破，是每天都會出現的混合狀態，那時 jade 主數字與紅色脂肪條本來就該同框。

### 字體

```css
--font-body: -apple-system, "PingFang TC", "Noto Sans TC", "Microsoft JhengHei", sans-serif;
--font-num: 'Archivo Num', -apple-system, "PingFang TC", "Noto Sans TC", "Microsoft JhengHei", sans-serif;
```

**中文與一般文字用系統字體。** 零 webfont、零 CDN——iOS 拿到 SF Pro、Android 拿到 Noto Sans TC，都是平台自己為螢幕 UI 調校過的字。層次靠字重與字級做，不靠字型。

**數字為主的顯示元素套 `--font-num`（Archivo，自架 woff2，v2.7）＋ `font-variant-numeric: tabular-nums`**。這條規則從 v1.2 的「數字用 `--font-body` 加 tabular-nums 就夠」改過來，原因是**實測發現 `tabular-nums` 對這批候選 UI 字體全部無效**——Google Fonts 提供的靜態字重實例不帶那個 OpenType feature，套了也不生效。十個數字的寬度差實測：Archivo 0.09px、Outfit 12.89px、Space Grotesk 8.88px、Manrope 9.68px、Sora 14.56px。Archivo 是唯一天生帶 tabular figures 的候選（不靠 `tabular-nums` 開關，寬度本來就一樣），**這是選它的唯一硬理由，不是好看**——換成別的字體，時間軸每列的卡數會左右跳，這條約束不可退讓。完整拉丁 woff2、不做數字 subset（省下的十幾 KB 換不到「不用建 subset pipeline」的代價），放 `src/assets/` 讓 Vite 處理。
**裝 400 與 600 兩個字重（v2.13）**。v2.7 上線時只抓了 600（「唯一實際套用的字重」），但沒設字重的次要數字元素（`.item .kc`／`.item .qty`／`.pair .tgt`／`.pair .sep` 這類「不是要盯著看」的數字）本來就該繼承環境的 400，只是當時 `@font-face` 只有 600 這一張字形可用，瀏覽器沒有更輕的可選，全部數字因此不分主次一樣粗，真機回報「字重應該要有分別」（2026-08-02）。補一份 400 的 `@font-face`，兩邊 CSS 規則完全不用動——沒特別設字重的元素字形一到位就自然分流。
**中文與數字「黏在一起」（數字被 CJK 字直接夾住，如「1863 卡」「30 分鐘」這類數字＋CJK 單位）不套** `--font-num`——兩種字體同框會讓同一句話看起來斷成兩截。
**日期標題是例外（v2.13 起套用）**：格式是「週日 8/2」，中文與數字中間隔一個空格，是兩個獨立區塊而非緊貼夾住，斷裂感的前提不成立；套用後全站數字顯示一致，使用者實機比對後裁定套用（Archivo 沒有 CJK 字形，「週日」兩字自動 fallback 回系統字體，不用拆 span）。

> 已實測：Windows（微軟正黑）tabular 生效；Archivo 實際載入用 `document.fonts.check()` 驗證過（非靜默 fallback）。iOS 真機上線後補驗一次。

### 字階（1.25 major third，全頁六級以內）

```css
--t-xs:   0.72rem;   /* 11.5px · 標籤 */
--t-sm:   0.8rem;    /* 12.8px · 品項熱量、次要說明 */
--t-base: 0.875rem;  /* 14px   · 品項名、內文 */
--t-md:   1rem;      /* 16px   · 餐別名、CTA、日期 */
--t-lg:   1.25rem;   /* 20px   · 登入頁問句（v2.0 新增，1rem 的下一級） */
--t-hero: 2.75rem;   /* 44px   · 剩餘熱量主數字（600 weight） */
```

### tracking（字距隨字級走，不是全域一個值）

```css
--track-hero: -.022em;   /* 44px 主數字 · 大字放著不收會散 */
--track-md:   -.008em;   /* 16px 餐別名、CTA、頂欄 */
--track-lg:   -.005em;   /* 20px 登入頁問句（v2.0 review 修正：原本借用 16px 的 --track-md，
                             20px 字階缺自己的 tracking 值） */
--track-base:  0;        /* 14px 內文 */
--track-xs:   .015em;    /* 11.5px 標籤與數值 · 小字放開才好讀 */
```

leading 反向：大字緊（主數字 `.95`、餐別名 `1.2`），內文鬆（`1.45`–`1.55`）。

### 間距（4pt）

```css
--s-1: 4px;  --s-2: 8px;  --s-3: 12px;
--s-4: 16px; --s-5: 20px; --s-6: 24px;
```

水平邊距固定 `--s-5`（20px）。

### 圓角與層次

```css
--r-btn:  16px;   /* 主 CTA、分頁列外框 */
--r-tab:  12px;   /* 分頁列內層 */
--r-pill: 999px;  /* 圓形節點、chip */
```

**不用陰影。** 分層靠 `--paper` / `--card` / `--raised` 的明度差。

## 元件規則

| 元件 | 規則 |
| --- | --- |
| 主 CTA | **56px 實心 jade 圓鈕、只有加號、無文字（v2.3）**，`--accent` 填色、`--on-accent` 圖示（加號 `stroke-width: 2.2`，見「圖示」條）、`aria-label="記一筆"`、**固定在底部列右端的拇指區不隨內容捲動**。v2.2 前是滿寬 52px 的「＋ 記一筆」文字條，**文案規則已作廢**——使用者裁決（2026-07-30）：這個 app 之後不只記飲食，屆時「記一筆」這個文案本身就會過期，通用的加號才撐得住新增其他類型的紀錄。**必須比同列的分頁大一級**（56 vs 44）：全站唯一的主動作不能與導覽同重 |
| 全寬動作鈕（`.action-btn`） | 登入頁與載入／錯誤畫面用的**滿寬 52px 文字鈕**（`--accent` 填色、`--on-accent` 文字、`--r-btn`）。v2.3 之前它與今日頁主 CTA 共用 `.cta`，主 CTA 改成 56px 圓鈕後兩者分家——那兩個畫面沒有底部列、也沒有「一列裡要分主次」的問題，滿寬文字鈕仍是對的（「用 Google 登入」「重試」都必須有文字） |
| 次要動作 | 不填色，`--accent` 文字 |
| 熱量量尺 | 高 8px、圓角 4px、軌道 `--rule`、填色 `--accent` |
| 三大營養素條 | 常態高 3px，**破表時長到 6px**；三等分橫排，各自「標籤 → 條 → `60.5/126` 數值」由上而下。三條底部對齊 |
| 破表狀態 | 填色 `--over-fill` 打底、疊 -45° 斜紋（`--over-stripe` 2px / 透明 4px，週期 6px）、文字換 `--over`。熱量尺高度不變（8px） |
| 破表的非顏色訊號 | 紅綠色覺者不靠顏色也要分得出來，靠**兩層**：主訊號是營養素條的 3px → 6px 高度差（三條並排時直接分成兩類），紋理是第二層。紋理內部對比僅 1.47:1，**不可單獨承擔辨識**——所以 3px 常態高度上不塗紋，塗了也糊掉 |
| 三大營養素判定 | **各自獨立**。脂肪、碳水 >100% 轉破表；蛋白質不足**不**轉紅（吃不夠是待辦，不是錯誤） |
| 數對（`已吃/目標`） | 一律 `900/1860` 這種形式，**不加空格、不加「已吃」「目標」標籤**。前後做階層，否則兩個語意不同的數字長得一模一樣：當前值 `--ink` ＋ 600（會變動、要盯的）／斜線與目標值 `--ink-muted`（固定參考值）。破表時**只把當前值染 `--over`**——目標值本身沒有超標。**視覺階層對螢幕閱讀器無效**，容器要有 `aria-label="已攝取 903 大卡，目標 1860 大卡"`，內部 span 一律 `aria-hidden` |
| 品項份量 | `qty ≠ 1` 時在品名後顯示 `×2`（`--ink-muted`）；`qty = 1` 不顯示。份量不寫進食物名——記錯份量是最常見的錯誤，畫面上看不到就查不出來 |
| 時間軸節點 | 已吃＝實心 `--accent` 圓 9px；待記錄＝空心、1.5px `--ink-faint` 邊框（`--rule` 對 paper 只有 1.37:1，圖形元素需 3:1）。連接線 1px |
| 時間軸虛實線 | 線的虛實**一律看下端節點**：下端已記錄＝實線，下端待記錄＝虛線。中間餐次空著（例如沒吃早餐但有午餐）時這條規則才有唯一解 |
| 時間軸節點對齊 | 圓點中心對餐別標題的**文字中線**，不是行盒。已記錄 `margin-top: 5px`；**待記錄要 `17px`**——它包在 44px 觸控行裡、文字垂直置中，沿用 5px 會差 12px。改任何相關行高後要重量一次（用 Range 量文字墨水範圍，別目測） |
| 餐別標題 | `--t-md` / 700，下方一條 1px `--rule` 細線與品項切開 |
| 品項列 | 用 `ul`/`li`，名稱 `--t-base` / 400 `--ink`；熱量 `--t-sm` `--ink-muted` |
| 待記錄餐次 | 整行可點（真 `button`），直接進該餐別的記錄流程。chevron 用 `--ink-muted`——`--ink-faint` 對 paper 只有 2.92:1，而它是這一行唯一的「可點」視覺提示 |
| 底部列（v2.3） | **CTA 與分頁併成同一列**：分頁 pill 靠左、56px 圓形主 CTA 靠右，各自貼 `--s-5` 邊距、垂直居中，整列約 72px。取代 v2.2 的上下兩段（CTA 區 68px ＋ 分頁列 70px ＝ **138px，佔 745px 可視高度的 18.5%**，真機體感「擠又高」）；合併後時間軸多出 **84px（實測 355→439）＝約兩列品項**。**不做「分頁置中 ＋ CTA 右下」**：393px 寬度下置中的 pill 右邊被圓鈕擠著，會讀成偏左而不是置中，兩個元素都靠中軸還互相打架 |
| 分頁列 | pill 收窄成內容自身寬度、**靠左（v2.3，取代 v2.0 的置中）**：外框與分頁一律 `--r-pill`、底 `--card`、1px `--rule` 邊框。**分頁改純圖示（v2.3）**——同列已有主 CTA，兩顆帶字分頁會把這一列塞滿；只有日記與設定兩項且齒輪語意明確，`aria-label` 補足無障礙名稱。分頁用真 `button` ＋ `aria-current="page"`、觸控 ≥44px。**選中態＝jade 圖示色 ＋ 1px `--accent` inset 邊框 ＋ `--accent-soft` 底（v2.4）**。底色從 `--raised` 換成 `--accent-soft` 的理由：拿掉文字標籤後，選中的 jade `#1C7059` 與未選中的 `#7A5C44` **亮度只差 1.02:1**（差異幾乎只有色相，對色弱者近乎不可辨），而 `--raised` 對 `--card` 只有 **1.14:1**，等於整個狀態訊號只剩那圈 1px 環在扛。`--accent-soft` 本來就定義為「選取底色」，不必新增 token。**但要誠實記帳：換底色買到的是色相差，不是亮度差**——`--accent-soft` 對 `--card` 實算只有 **1.22:1**（換之前的 `--raised` 是 1.14:1），而 jade 圖示對底色反而從 5.01:1 微降到 4.67:1（兩者都過 3:1）。所以另外加**非顏色訊號：選中的圖示描邊 1.6 → 2.2 加粗**，同「破表靠 3px→6px 高度差當主訊號、顏色是第二層」的原則。只有色相差的狀態訊號在色弱者眼裡等於沒有 |
| 捲動範圍 | 只有時間軸的品項區捲。頂欄／額度／營養素／CTA／分頁列固定不動。捲軸隱藏（`scrollbar-width: none`），手機本就是 overlay scrollbar |
| 區塊分隔 | **不用 1px 硬線**。時間軸頂端用 10px 漸層遮罩（`-webkit-mask-image` ＋ `mask-image`，前綴不可省，舊 iOS Safari 會失效）。**底部刻意不做漸層**——捲到底時會把最後一項淡掉，而被切的文字本身就是「還有內容」的訊號 |
| 觸控區 | 一律 ≥44×44px。待記錄整行、分頁 44px；主 CTA 56px 圓 |
| 圖示 | 全部 inline SVG，`stroke-width: 1.6`、`fill: none`、`aria-hidden="true"`。**唯一例外：主 CTA 的加號用 2.2**——18px 圖示疊在 accent 填色上，1.6 撐不住。不用 emoji、不用 icon font |
| 進度條數值 | 寬度一律 `clamp(0, x, 100)%`——碳水 227.9/214 是 106.5%，不夾住會爆版。**分母為 0 或 NaN 時走降級畫面，不要把 NaN 送進 DOM** |
| 小數捨入 | **只在最終顯示時捨入，小計一律由未捨入值加總**。營養素顯示一位小數、熱量顯示整數；各自捨入再相加會讓餐別小計與主數字對不起來，天天看一定會發現 |
| 破表臨界 | `>` 才算破表，`==` 不算——「超出 0 卡」看起來像 bug。判定用**捨入後**的值，否則 1860.4 對 1860 會判成破表，但畫面上兩個數字長得一樣 |
| 主數字三數自洽 | 已吃、目標各自捨入後，剩餘由**捨入後的值相減**。直接捨入未捨入的差會讓 `903 + 957 ≠ 1861` 這種對不起來的組合出現在同一屏 |
| 載入態 | 主數字停在 `—`，時間軸顯示「載入中…」。**不得先渲染 0 或目標值再跳成真值**——「三秒瞄一眼」剛好會看到假數字。「讀到空」與「還沒讀到」必須在畫面上分得開。**也不得拿前一天的數字撐場面**（同一條規則的另一半：那是「已經讀到、但讀的是別天」）。**v2.5 例外：切到已在快取的日期時完全不進載入態**——那不是「還沒讀到」，資料已經在手上，此時顯示 `—` 反而是假的 |
| 日期切換的體感 | **快取 ＋ 預取相鄰日（v2.5）**：看過的日期進記憶體快取；載入某天成功後在背景預取前一天（非今日時連後一天，但不預取未來）。真機回報「切日期要 loading、讀太久」——那 ~300ms 是實打實的 Supabase 往返，優化不掉，能拿掉的是「使用者在等」，所以按下箭頭時資料要已經在手上。實測（人工加 300ms 延遲）未命中 479ms、命中 149ms。**不做背景重新驗證**：只有一個使用者、資料只從這支 app 改，多一條非同步覆寫路徑就多一個蓋掉樂觀刪除的機會。**任何把 rows 放進畫面的入口都必須共用同一套「待刪濾除」**（快取命中、fetch 回來、預取回填），漏一條就會出現「已刪的那筆復活」 |
| 空狀態（每天早上） | **不做 0% 的最小可見態、不填那段空白**。數字（`1860`、`0/1860`）與四個空心節點已經讀得出「今天還沒開始」；補文案解釋已在下方否決，補假的進度條像素則是騙人 |
| 登入頁 | **極簡（v2.0，Meta splash 式）**：畫面中心＝48px 幾何 T mark（jade）＋主角問句「今天吃了什麼？」（1.25rem / 500 / `--ink`、與 mark 間距 `--s-4`）；CTA 沉到**底部拇指區**（同 app 內「CTA 固定在拇指區」原則）。此屏只在首次使用或登出後出現——session 會持續，平常打開直接進今日頁。**不放 Google 彩色 logo**——fill-based 圖示與「全部 inline SVG、`stroke-width: 1.6`、`fill: none`」的規則相斥，也省掉 Google 品牌指南的一串約束 |
| Wordmark | **全產品不出現（v2.0 裁決）**。品牌只靠幾何 T icon；「Tally」字樣只活在 HTML `title` 與主畫面 app 名稱。連帶：wordmark 字體題作廢——當時「零 webfont、零自架字體」維持無例外，**v2.7 起改為「零 webfont（CDN 載入），數字元素允許自架」**，見「字體」章節 |
| App icon | 方向 3 幾何 T，**v2.5 反轉配色**：`--accent` 實心底 ＋ `--card` T（1024 稿座標不變：橫 bar x162 y252 700×180 rx60、豎 stem x452 y252 220×520 rx60）。輸出 180px PNG 當 `apple-touch-icon`，favicon 用同一張圖形（favicon 保留 rx224 圓角，瀏覽器分頁不套遮罩）。60px 是驗收點——縮到書籤大小仍讀得出 T。**PNG 一律方形滿版、不預先切圓角**：iOS 對 web clip 會套自己的 squircle 遮罩，PNG 自己先切一次等於兩層圓角疊在一起，透明角在暗色桌面上被合成成暗色，整顆看起來偏濁（真機回報「暗色模式下 T 的綠太深」的可能成因之一）。反轉的另一個好處是暗桌面上的存在感——淺色 T 在深色底上不吃桌布 |
| 設定頁（第一版） | 唯讀 `dl` 清單：今日目標四項 ＋「怎麼算出來的」（體重／量測日／身高／年齡／BMR／活動係數／TDEE／目標／三大比例）＋ 兩個次要動作（記體重、編輯身體參數）＋ 登出。**目標怎麼來的要看得到**——1860 是算出來的，看不到算式就只能相信它 |
| 設定頁的編輯與記體重 | 都走**同一套 sheet**，不另做頁面。身體參數：生日／身高＋性別／目標＋活動係數／三大比例三欄，欄位規則同新增食物（floating label）。三大比例相加必須 100，前端先擋（DB 的 check 是最後一道）。記體重：量測日預設今天、體重必填、體脂選填；同一天再記是**覆蓋**（`unique(user_id, measured_on)`，寫入帶 `Prefer: resolution=merge-duplicates`）。兩者存檔後重算目標並**留在設定頁**，不跳回今日頁 |
| floating label 的高度 | 欄位 60px、上內距 26px、label 浮起後 `top: 8px` ＋ `line-height: 1`。**這四個值是一組的**：樣張原本的 52/18/13 會讓 label 行盒與輸入文字重疊（實測重疊 4.7px），字擠成一團。改動任一個要重量 label 底緣與文字起點的距離（目前 7.3px） |
| 表單控件字級下限 | **一切可聚焦輸入（input／select／textarea，含搜尋框、份量框、日期框）字級一律 `--t-md`（16px），不得低於**。iOS Safari 對 <16px 的輸入框聚焦會自動 zoom，heuristic 關不掉；`maximum-scale=1` 能壓但違反 WCAG 1.4.4，不走（真機實測撞到，2026-07-29） |
| 記一筆 sheet | 走**同頁覆蓋層**（sheet），不做獨立頁——獨立頁等於整頁重載加重打 API，還得為今日頁做本地快取。清單這一屏**沒有標題文字**：chip 列的選中態已經說了是哪一餐，再寫一次是同一個資訊講兩遍；關閉鈕併進 chip 列同一行，省下的 52px 在 745px 的畫面裡是多看到一整列食物。容器用 `aria-label` 提供無障礙名稱（`role="dialog" aria-label="早餐"`）。**新增食物／記體重／身體參數三張表單相反，要顯示標題**——它們沒有 chip 給情境線索，拿掉就不知道自己在哪 |
| sheet 版面分區 | 固定不動、由上而下：餐別 chip 列、搜尋框。**唯一捲動區**：已選段落（有選取才出現）→ 該餐別常吃 → 全部食物。底部固定：確認列（有選取才出現）。**sheet 只負責新增，不負責刪除**（刪除入口見下方「今日頁品項刪除」） |
| 記一筆入口 | 兩個：今日頁主 CTA「記一筆」（餐別未定，chip 依時間預選）；時間軸的餐別標題列或待記錄行（餐別已定，直接開對應餐別的 sheet）。主 CTA 文案維持「記一筆」 |
| 食物清單多選 | **可多選**——使用者平日早餐固定三樣，逐一新增要走三趟流程。未選取列右側顯示單份熱量；選取後同一位置換成加減按鈕組（減號、數字、加號），不再顯示熱量 |
| 加減按鈕組 | 加減鈕各自 ≥44×44px；**數字本身可點**，點了叫數字鍵盤填精確值（份量可小數）；整組寬度上限 130px |
| 選取態視覺訊號 | **不鋪底色**。訊號是勾選圖示變實心＋右側長出加減按鈕組——加減鈕組出現本身就是最強的差異，三列連排鋪 `--accent-soft` 會與 cream 背景形成大色塊（見下方「已否決的做法」） |
| 已選段落 | 在捲動區**最上方獨立成一段**，不隨餐別切換而消失，可直接就地改份量或取消。被選取的項目從常吃或全部食物清單**移除，不重複出現**——同一項目在一屏出現兩個可互動實例是同步與混淆風險 |
| 切換餐別（已選） | 不清空已選項目，已選跟著搬到新餐別 |
| 逐筆超標預警（v2.10，v2.11 收窄／訂正） | 記一筆 sheet 瀏覽清單裡**每一筆未勾選的食物**（勾選後改顯示份量 stepper，沒有空間再放預警，且它的貢獻已經算進 `combined` 與底部確認列的 `(+N)`，不會漏講）即時試算：`(今日已吃 ＋ sheet 內已勾選加總) ＋ 這一筆本身 × 該食物在這一餐上次吃的份量`對 `targets` 的熱量／脂肪／碳水**分別**比較（**不含蛋白質**——它的語意是「達標」不是「超標」，另外走「底部確認列」條）。**份量用 `defaultQty`（勾選當下實際會用的量），不是固定 1**——v2.10 原文沒定義份量、實作默默假設 1，跟 `togglePick` 真正勾選時用的量對不上，會出現「預告不超標、點下去卻超標」（precommit review 2026-08-01 抓到）。`.food-row .kc` 數字只反映**熱量**本身會不會被這一筆推過線，會才變 `--over`；下面新增一行小字（同色），**只列出脂肪／碳水**——**v2.11 拿掉熱量**：真機實測回報「460」（這筆自己的熱量）跟下面「+679卡」（推過線後總共超出多少）兩個數字都掛著「卡」字，讀起來像同一件事講兩次；熱量超標已經有 `.kc` 變色（這筆）與底部確認列的 `(+N)`（總計）兩處在講，delta 這裡再列一次是三重複。**視覺（`formatOverDelta`）與螢幕閱讀器（`formatOverAria`）分家**：前者不含熱量（因為有顏色可以借），後者三項照唸（螢幕閱讀器使用者沒有顏色可以借，一樣要聽到熱量超標）。**不分餐別**——公式跟吃哪一餐無關，早／午餐額度通常寬鬆、不太會觸發，不必另外排除。**零新增顏色**：全部複用既有 `--over` |
| 全部食物／搜尋結果排序（v2.15，視覺降權在 v2.17 拿掉，見版本紀錄） | 使用者提需求「以今天剩餘的空間，我還能吃什麼」，比過三個方案（A 硬篩選會把清單清空、B 軟性排序、C 門檻式）的本機 demo 後裁決 **B：不隱藏任何品項**，符合今日剩餘額度的排前面，不符合的留在清單但降視覺優先度——結構上不可能清空清單。**全部食物**與**搜尋結果**兩段依「加上這一筆（`defaultQty`，跟逐筆超標預警同一份量基準）今日已吃還吃得下嗎」排序：符合的排前面（維持原本 byName 順序），不符合的排後面、按超出總量（`over.kcal + over.fat + over.carb`，三個單位不同但只拿來排序不顯示）由小到大。**視覺降權機制試過兩版都被否決**（opacity 為過 WCAG AA 被迫淡到看不出效果；背景色調跟「不鋪底色、靠明度分層」的極簡風格不搭，兩次真機核可都被打回，過程見版本紀錄 v2.15/v2.16/v2.17）——**v2.17 定案不做任何額外視覺處理**，`.over-quota` class 只當排序/測試用的語意標記，不掛樣式；不符合的品項純粹靠位置（排後面）與既有的 `.kc.over` 紅字／`.kc-delta` 超出量文字被看出來，仍可正常點選加入，`aria-label` 不受影響。**排序判斷基準刻意用「今日已吃」（`eaten`），不是「今日已吃 ＋ sheet 內已勾選」（`combined`）**——後者會讓使用者每勾一個東西整批清單重新洗牌一次，手指還沒點完下一個就跳位、容易誤觸；逐列變色與 `kc-delta`（既有的逐筆超標預警機制）維持用 `combined` 即時反應，兩者刻意不同源、互不干擾。**「常吃」不套用排序**——它只有最多 5 筆，語意是「這餐平常都吃這些」，重排會打壞這個用途，清單順序完全跟著既有的 recency（`recentIds`）不動。判定精度沿用 `rowOverage` 既有的 0.1 級（`roundTo1` 後比較），跟逐筆超標預警同一把尺，不另開一套 |
| 底部確認列 | 小計靠左（例如 `3 樣 · 363 卡`）、加入後剩餘靠右（`剩 594`），中間留白分開；下方整條加入按鈕。**不加「已選」「還能吃」這類標籤字**——數字自己會說話。數對階層照既有規則：小計 `--ink` ＋ 600（會變動、要盯的），剩餘 `--ink-muted`（固定參考值）。**v2.10：熱量超標時右側「剩」讓位**——改成緊跟在小計數字後面用 `(+40)`（`--over` 色）標出超出多少，右側因此空出來；未超標維持原樣，右側照舊顯示「剩 X」。**新增第二行：蛋白／脂／碳三個誠實數字**（格式同今日頁 `82/126`，見「數對」條）。**v2.11 訂正**：這裡原本寫「三個都不做顏色判定」，是誤把「蛋白質不套達標色」這條錯擴大成「三個都不套色」——正確規則跟今日頁三大營養素條**同一條**（見「三大營養素判定」）：**脂肪／碳水各自獨立 >100% 轉 `--over`，只有蛋白質不足不轉紅**。真機實測使用者發現脂肪碳水超標卻沒變色，回報「這是不是漏了」，查證確實是實作誤讀自己寫的規則，不是新決策 |
| 加入成功 | sheet 自動關閉回今日頁，新品項在時間軸上以 `--accent-soft` 短暫高亮後淡出，只動 `opacity` 與 `transform`，遵守 `prefers-reduced-motion`。**峰值 `.55` opacity、`700ms`、`--ease`（v2.13）**——原本 `.9` opacity 撐 `1.2s` 才用 accelerate 曲線衰減，讀起來是「整列被實色薄荷綠淹住不動、最後暴斃」的舊式成功提示，改成低飽和的漸緩淡出 |
| 寫入失敗（記一筆） | 錯誤訊息**就地可見**，已選內容不清空，按鈕換成「重試」。不做離線佇列（YAGNI）——失敗要當場看得見即可 |
| 搜尋比對範圍 | 同時比對**品名與店家**——使用者有五筆「雞胸餐盒」只靠店家區分，品名裡的店家標記與 vendor 欄格式不一致，只比對品名找不到 |
| 搜尋框 | 是**模式切換器**：沒輸入時清單顯示該餐別常吃接全部食物；一有輸入，搜尋結果取代整段清單，兩段標題都不出現。同時只存在一段清單 |
| 新增食物入口 | 搜尋框只要有輸入，結果清單末尾永遠有一列「新增『輸入的字』到食品庫」；零結果時它是唯一一列。**不能只在零結果時才出現**——搜到部分結果時使用者一樣可能需要新增 |
| 切換餐別（搜尋） | 不重置搜尋關鍵字 |
| 新增食物表單 label | **floating label**：label 是真的 `label` 元素，初始在框內看起來像 placeholder，聚焦或有值時縮小上移到框線上。純 CSS 用 `:placeholder-shown` 與 `:focus` 加 `transform`，不寫 JS。**禁止用 placeholder 冒充 label**——一輸入就消失、對比度不足、螢幕閱讀器支援不一致，是 WCAG 3.3.2 常見失敗案例 |
| 新增食物表單欄位 | 三行：品名與店家並排、熱量整行、蛋白質脂肪碳水三欄並排（順序固定）。**沒有分類欄，沒有份量單位欄**。必填只有品名與熱量；**三大營養素留空當 0**——無糖飲料的蛋白質與脂肪本來就是 0，不該逼人打三個零。熱量不給這個待遇：它是這裡唯一的核心數字，忘了填那筆等於沒記 |
| 食物清單列顯示 | 品名為主，店家在下方第二行用 `--ink-muted`；沒有店家就沒有第二行。**店家那一行不可截斷或省略**——它是區分同名品項的唯一資訊 |
| 店家欄位（v2.6） | **可自由輸入的 Autocomplete**（Base UI `Autocomplete`），不是受限 `Combobox`——使用者常打錯店家名，逼著只能從清單選反而製造新的挫折，這裡要的是「既有的選得到、沒有的也送得出去」。選項來源＝ `foods` 上的 `vendor` 字串去重排序（`localeCompare(..., 'zh-Hant-u-co-stroke')`，明釘筆畫排序而非依賴引擎預設——V8／WebKit 實測預設已等於筆畫，但明釘避免未來換引擎跑出拼音序），**不建專屬資料表**——去重就是清單，建表要付新表／外鍵／遷移／RLS 的代價卻買不到東西。外觀沿用 `.field-float`（`Autocomplete.Root` 不渲染自己的元素，`input` 仍是緊接 `label` 的直接子節點，floating label 的 CSS 選擇器照樣命中）。**下拉的 `Autocomplete.Portal` 一定要把 `container` 指到 `Drawer.Content`（LogSheet.tsx 的 `sheetRef`），不能用預設的 `document.body`**——vaul 的 Drawer 底層是 Radix Dialog，開啟時把 `body` 設成 `pointer-events: none`、只放行 Dialog Content 自己的子樹；掛在 `body` 下的選單看得到但點不到，兩層 portal 疊在一起就是這裡最容易翻船的地方（真機／e2e 都撞到過）。指到 `sheetRef` 之後選單變成 `.sheet` 的子孫，**因此不需要任何 z-index**——`.sheet` 底下沒有其他子層自己設 z-index，選單靠 DOM 順序（Portal 內容排在最後）本來就疊在最上面。曾經留過一條防禦性的 `z-index: 1`，precommit review 指出它沒有任何行為依賴、也沒有情境驗證得了它在防什麼，2026-07-31 移除。IME 組字沿用 Base UI 底層共用的 `AriaCombobox`（跟 `Combobox` 共用一份 composition 處理），不需要再比照食物搜尋另外做 query/filterQuery 雙 state。**`Autocomplete.Empty` 不管有沒有結果都留在 DOM 裡（`aria-live` 要用），有結果時沒有子節點但自己的 padding 還在，變成清單上方一段死留白——`.vendor-empty:empty { padding: 0 }` 收掉（v2.13）**。**新增食物表單進場焦點落在店家欄位，不是熱量**——品名已經由搜尋字串帶進來，下一個該填的是店家（v2.13） |
| 今日頁品項刪除入口 | 刪除在**今日頁，不在 sheet**——看到錯的那筆就在原地刪，不必先想它屬於哪一餐 |
| 今日頁品項刪除手勢 | **v2.1 改版**：`motion` drag 手刻（v2.0 的 `react-swipeable-list` 已退場，理由見下方版本紀錄）。結構兩層：`.item-delete` 固定壓在列右緣底下，`.item-slide` 是唯一會動的層，只寫 `transform`。左滑時該列底色 `--card` → `--raised`。開啟 `--dur-base`、關閉 `--dur-mid`。**開合狀態只有一個來源**（`openId`）：觸控與鍵盤兩條路徑共用，「開一列自動關他列」是單值 state 的自然結果，不必另外維護 close 函式表。**v2.9：某餐最後一筆刪除時的退場動畫**——Today.tsx 拆出 `MealNode` 元件，用 `lingering` state 讓「翻成待記錄列」延後到 `AnimatePresence` 的 `onExitComplete` 才發生，不再跟著 `items.length` 同一個 render 就切掉整段。原本 `done = items.length > 0` 一格公式在該餐只有一筆時會讓 `AnimatePresence` 連退場動畫的機會都沒有就被卸載（實測：刪除後 10ms 內畫面已經是「待記錄」，`.item` 完全沒有 opacity 過渡），2 筆以上互刪則不受影響（`hasItems` 全程是 true，不觸發 lingering 分支） |
| 滑到底刪除 | 拖過列寬 **45%** 放手＝直接刪除（iOS 提醒事項行為）。越過門檻時紅圓 `scale(1.1)` 預告，讓它不是盲的。**這條的前提是 undo 存在**——沒有復原就不該讓單一手勢造成不可逆的資料損失 |
| 刪除的復原提示 | 按下刪除**先不打 DELETE**：該列樂觀移出畫面，**底部列上方浮出一顆 44px 深色 pill 並置中**（`--ink` 底、`--paper` 字、undo 箭頭＋「復原」，整顆可點），距底部列 `--s-2`，**5 秒**後才真的送出。復原＝把刪除前的那一筆插回目前清單，不做反向 INSERT（會拿到新 id，且復原本身也可能失敗）。換日期、記一筆、切分頁、頁面隱藏（`pagehide`／`visibilitychange`）都先結清待刪。**用絕對定位不走版面流**——走流的話它一出現就把時間軸擠短 62px、消失再彈回來（實測每次刪除都抖一下）。**底色必須是深色**：`--raised` 對 `--paper` 只有 1.14:1，真機上看不出畫面多了東西；jade 則會與正下方的 CTA 撞成兩塊同色。畫面上不寫「已刪除」（列消失就是回報），只給 `role=status` 的 sr-only 文字。**v2.3 位置改置中，且不進底部列**：底部列左端是分頁、右端是 CTA，中間雖然空得下它，但它是 5 秒就消失的暫時物件——進常駐列會讓每次刪除都改變那一列的構成，消失時還留一塊空白抖動；置中且不與左右任一端對齊，位置本身就在說「我不是這一組的」。CTA 改純圖示後，它是畫面上唯一帶文字的浮動元素，反而更好認 |
| 刪除的鍵盤路徑 | 手勢對鍵盤與螢幕閱讀器是死路，所以品項內容本身是原生 `button`，點擊它也會露出刪除鈕。左滑與點擊兩條路都通 |
| 刪除鈕樣式 | **iOS 提醒事項風格（v2.0）**：紅底 `--over-fill`、白色垃圾桶 inline SVG、有 `aria-label`。不放文字——品項列實測高度 44px，圓已吃滿，塞「刪除」兩字只能縮圖示。**盒子 44px（觸控區），紅圓畫在 `::before` 上 inset 2px＝40px（v2.2）**：圓畫在盒子本身時上下緣與列相切，門檻預告一放大就被 `.item-row` 的 `overflow: clip`（修水平溢位用的）切掉上下各 4px，真機看起來是「圓角過大又被截斷」。40px 打底後預告 `scale(1.1)` 剛好 44px 貼齊列高 |
| 今日頁品項編輯份量（v2.14） | 觸發是**長按品項**（`item-content`，pointerdown 起 500ms 計時）——刻意不擴寬 `item-content` 塞第二顆按鈕、也不動左滑刪除那組手勢（`REVEAL`/`OPEN_AT`/`FULL_AT`），長按與 drag 手勢是兩條獨立路徑，只在「互相取消」上耦合。**取消門檻實際上分兩層**：真的觸發 framer 的拖曳辨識（`onDragStart`/`onDrag`，內部位移門檻約 3px）會立刻取消，這是絕大多數情況下實際生效的那一道；另外自己在按鈕上補一道 10px 的 `pointermove` 位移偵測當備援，只在 framer 完全不介入的原生垂直捲動（`dragDirectionLock` 放行、不觸發 `onDragStart`）才會派上用場。開啟一個自建 sheet（同 Settings.tsx 的記體重／身體參數 sheet，不走 vaul）：標題「編輯份量」、內容沿用「記一筆」sheet 的 qty stepper（`.qty-stepper`，見「加減按鈕組」條），只改 `qty` 這一欄，不重算 kcal/protein/fat/carb（intake 存的是單份快照）。存入成功後畫面立即反映新值（品名旁 `×N`、該筆熱量、該餐小計、額度提示皆重新計算），不重新整理頁面。失敗時 sheet 內就地顯示「存不進去：」，不關閉、不清空輸入，跟「記一筆」的失敗處理是同一套慣例。**長按計時器隨 `SwipeRow` unmount 清除**——沒有這道清理，計時器會在那一列已經從畫面上消失後（換日期、刪除退場動畫跑完…）繼續倒數，時間到了照樣對一筆已經看不到的品項開 sheet、寫進 DB，畫面卻停在切走前的快照（fresh-context verifier 收案前抓到，2026-08-02） |
| 日期切換 | 不用原生 `input type="date"`（系統日曆突兀）、不做自製月曆（記帳只往回翻幾天，YAGNI）。格式「週二 7/28」（星期＋月/日、無年份），右箭頭在今天**停用**（不能看未來）。日期文字是靜態文字，不當按鈕。**v2.4：三者包在一顆膠囊裡**（`--card` 底、1px `--rule` 邊框、`--r-pill`，整顆置中；箭頭在膠囊內 40px 寬 × 44px 高，日期左右內距 `--s-2`）。裸 chevron 在 cream 上沒有任何「可按」的訊號，而 iOS 26 把導覽列裸圖示鈕包進容器正是為了這件事；**但不照抄它的玻璃材質**——我們是平的 cream，容器只能靠 `--card` ＋ 1px 邊框，而 `--card` 對 `--paper` 僅 1.05:1，所以這顆膠囊是安靜的，不與 44px 主數字搶重量。**已否決：兩顆箭頭各自包一顆 32px 圓**——兩個小容器黏在標題兩側，讀成「標題旁邊黏了兩塊東西」，而且把視覺重量放在全畫面最低頻的控制項上。**v2.3 改版**：`‹ 週二 7/28 ›` 三件一組**置中**在頂欄，箭頭緊貼日期左右兩側——**方向要對得上語意**，按左箭頭是往前一天，它就該長在日期左邊；v2.2 的「箭頭貼版面最左」與樣張 H3 的「箭頭成對收右」都被否決，後者是「看著左邊按右邊」（使用者裁決 2026-07-30）。**日期本身就是 h1**——v2.2 的靜態頁名「日記」已拿掉：分頁列已經寫了「日記」，它佔一行卻不帶資訊。**v2.7：字級改 `--t-md`（16px）/600**（原 `--t-lg` 20px/700，使用者實機回報「有點大、有點突兀」）——日期是導覽元素不是標題，主角是下面 44px 的主數字；20px/700 跟頂欄左端 12.8px 的「回今天」落差太大，是突兀感的來源，見版本紀錄。箭頭在頂欄不進拇指區是**刻意的**：99% 的使用是記當天，切日是低頻，把它放進拇指區等於讓每天最常按的位置被每週用兩次的東西佔著 |
| 回今天 | 與日期**同一行**的安靜文字鈕：純文字「回今天」、`--accent` 色、無底無框、padding 撐 ≥44px 觸控區；只在歷史日出現。**必須絕對定位**（走版面流會把置中的日期組推偏），今天時**同位置不放東西**。**v2.4：貼左端，不貼右**——貼右時它與「後一天」箭頭只隔 8.03px，日期字變長（「週三 12/29」多一位數）會縮到約 2.5px，破 Material 的相鄰觸控目標 8dp 地板。依據是 2026-07-30 的產品調查（7 個產品 ＋ HIG/Material）：**沒有任何手機觸控 app 把文字型「今天」與方向箭頭小間距並排**，唯一先例 Apple Calendar Mac 是滑鼠情境；主流解法是放在頂欄**對側**（Google Calendar iOS 把 Today 放右上、日期導覽放左上，物理上不可能誤觸）。**前置 chevron 一併拿掉**——移到左端後它與膠囊內的 `‹` 同向重複。**已否決的兩案**：改滑動日期橫條（Fantastical／MyFitnessPal 的做法）＝換掉整個互動，YAGNI；點日期本身回今天（Fantastical／Day One）＝違反「日期文字是靜態文字」那條，且畫面上沒有任何訊號說標題可點 |
| 歷史日記一筆 | 在歷史日按「記一筆」，記到當前檢視的那一天 |
| 歷史日主數字 | 改顯示**攝取量**（例如 `1266`），下方數對 `1266/1860`，標籤從「還能吃」換成「攝取」。版面與今天完全一樣，只換數字與標籤——回頭看過去問的是「那天吃了多少」，不是「還能吃多少」 |
| 「今天」的定義 | 就是系統日期，**不做凌晨分界**。跨午夜的宵夜由使用者自己切到前一天補登 |
| 熱量單位 | 全站用中文「**卡**」，不用 kcal。畫面顯示「卡」，`aria-label` 寫「大卡」 |
| 剩餘／超出的符號 | **不用正負號**（`+594` 會被讀成「多攝取 594」）。維持「剩 594」與「超出 193」兩種寫法 |

## 版面高度（實測，別用螢幕高度設計）

「一屏」指的是**瀏覽器可視高度**，不是裝置螢幕高度。iPhone 14 Pro 螢幕 844px，但 Safari 有網址列＋工具列時只剩約 **745px**。

- 實作用 `100dvh`，**不要用 `100vh`**（iOS 會被工具列吃掉）
- 底部固定區加 `padding-bottom: env(safe-area-inset-bottom)`，並在 viewport meta 加 `viewport-fit=cover`，否則 home indicator 會壓住分頁列
- 設計基準＝ **745px**。實測：四餐全記完的破表屏在此高度溢出約 96px，**這是預期的**——那時使用者已經吃完，時間軸是回顧性質，捲動可接受
- 但常態屏（有待記錄餐次）在 iPhone SE 這類小螢幕會把待記錄行推出視野。功能不會斷（CTA 是通用入口），只是少一步捷徑

## 語意與鍵盤（實作不可省，樣張已示範）

顏色字級間距寫得再細，這一節缺了就會做出鍵盤用不了的頁面。

- `<html lang="zh-Hant">`，否則 VoiceOver 會用系統語言唸中文
- **可點的東西一律用原生 `button`**，不要 `div` ＋ `cursor: pointer`——後者鍵盤 Tab 跳不到、螢幕閱讀器讀不出角色，且 iOS Safari 對非互動元素的 `:active` 不保證觸發
- 每個可聚焦元素都要有 `:focus-visible`
- 圖示按鈕要有 `aria-label`；純裝飾 SVG 一律 `aria-hidden="true"`
- 清單用 `ul`/`li`，讓 rotor 能逐項跳
- landmark：`header` / `main` / `nav`，頁面要有一個 `h1`
- 純視覺的進度條 `aria-hidden`（數值已由旁邊的數對文字提供，不必讀兩次）
- 主數字接上資料後加 `aria-live="polite"`，讓記錄完成有聽得到的確認
- `:hover` 包進 `@media (hover: hover)`——iOS 有 tap 後 hover 卡住的已知行為，會讓元素看起來像被 disable
- 重置樣式的 `button` 要補 `-webkit-appearance: none`

## 互動與動效

- 只動 `opacity` 與 `transform`，不動 layout 屬性
- `160ms cubic-bezier(0.4, 0, 0.2, 1)`——這是**按鈕級**規格（狀態變化、透明度）
- **sheet 進出場是唯一的例外**：`--dur-sheet`（280ms）`cubic-bezier(0.32, 0.72, 0, 1)` 進場、`--dur-mid`（220ms）同曲線退場——退場比進場降一級（原 200ms 真機體感「像直接消失」，2026-07-29 依已核准的 v2 動效階梯改 220 並落地 token）。**scrim 退場用 `--dur-fast`（100ms），必須比 sheet 短**：兩者同速時 sheet 滑走「掀開」的區域露出淡到一半的暗幕、頂部露出帶卻從全黑開始淡，真機看起來是分區變色（2026-07-29 實測）。四級階梯（100／160／220／280）自 v2.0 起全站鋪開：左滑刪除開 `--dur-base`、關與關他列 `--dur-mid`。
  按鈕級的 160ms 套在 650px 的位移上會像被彈出來；抽屜類元件需要更長的時間與更早煞車的曲線。
  進場動畫只在**真正開啟那一次**掛上，切餐別／切 view／載入完成一律走增量更新不重建外殼——
  外殼一重建動畫就從頭再播一次。退場要等 `animationend` 才清 DOM，並留一個計時器 fallback
- CTA 按下 `translateY(1px)`
- **每個可點元素都要有 `:active`，不能只有 `:hover`**——手機沒有 hover，只寫 hover 等於按下去毫無回饋。待記錄行 `.5`、分頁 `.6`、日期箭頭 `.5`。回饋在按下的當下發生，不等放開
- `:focus-visible` 2px `--accent` 外框、offset 3px，**即時出現不做過場**
- `prefers-reduced-motion: reduce` → 全部降到 `0.01ms`，CTA 位移取消
- **破表狀態切換不做過場**：3px → 6px 是 layout 屬性，直接跳，不補 transition

## 禁止事項

本專案的黑名單，每一條都有踩過的理由：

- **不用環圈進度。** 圓環從 Apple Watch 三環來，那裡「填滿＝達成＝好」；吃東西相反，吃到目標不是成就、吃爆才是問題。用橫向量尺。
- **不做暗色模式。**（2026-07-27 決定，只維護淺色一套）
- **不做綠色「達標」狀態。** 綠色若代表「在範圍內」，一天中多數時間都會亮著，常態的顏色沒有資訊量。且使用者 Notion 六天資料裡蛋白質從未達標，那會是死功能。
- **不替使用者預先分配剩餘額度。** 曾把剩餘 957 切成「晚餐 670 / 點心 287」，結果使用者得在心裡反推「不吃點心就能吃 957」——發明一個規則再要人反推它，是設計失敗。改為顯示共用總額。
- **CJK 不用 italic。** 強調靠字重與 `--accent`。
- **不從第三方主機載入資源**（webfont、CDN 的 CSS／JS、外連圖）。webfont 會讓首屏閃字（FOUT）並多一次 DNS＋TLS 往返，而這個 app 的情境是一天開五次、每次三秒；CDN 函式庫則是拿可用性與隱私換一點方便。**資料 API 不在此列——Supabase 就是後端。** 此條 v1.5 前寫作「不用任何外部請求」，字面上把後端一起擋掉了；「該不該多一個依賴」是另一個問題，各自論證，別假借這條。
- **不用陰影分層。**
- **不硬編色值、字級、間距。** 一律 `var(--token)`；需要新值先加 token。
- **不照抄錨點。** 「小資算熱量」是情緒參照，不是版型範本。

## 已否決的做法（避免重蹈）

| 做法 | 否決理由 |
| --- | --- |
| 沿用錨點的卡片堆疊版型 | 環圈佔滿第一屏只傳達一個數字；最高頻動作「加入食品」藏在捲動後面；空狀態各佔一整張卡 |
| 環圈當主視覺 | 見上方禁止事項 |
| 把「已攝取」放在主數字位置 | 打開 app 要問的是「還能吃多少」，不是「吃了多少」 |
| 飲料點心併進正餐 | 使用者六天每天都有一筆點心，是固定習慣；併入就再也看不出點心吃掉多少——那正是減脂最該單獨盯的一類 |
| 用 11px 的「!」小圖示標示破表 | 實測太小，看起來像引號。改用斜紋 |
| 斜紋用實色 + 亮條（`#B8533A` 4px / `#C06A50` 4px） | 在 8px 熱量尺上像斑馬線，跟 cream 的溫度打架；更嚴重的是 3px 營養素條上完全糊成純紅，「不靠顏色也能辨識」等於只在一半的元件上成立 |
| 目標刻度線 + 只有溢出段帶紋（表示超出比例） | 旁邊 `超出 229 卡` 已經把量講完，條上再編碼一次是重複，換來的是兩段寬度與刻度線定位的實作成本 |
| 靠垂直細紋（90°）撐住 3px 條 | 垂直線確實不受條高影響，但 3px 上只是「勉強看得到」，撐不起無障礙底線。改用高度差當主訊號 |
| 自架 subset 數字字體（2026-07-28 否決，**v2.7 已推翻**） | 2026-07-28 比過六種＋一種混用（Inter / IBM Plex Mono / Newsreader / Cubic 11 / Cubic 11+Inter）。當時的否決理由是「一致性換不到足夠價值，系統字在各平台拿到的都是該平台調校過的最佳解」——**這個假設 v2.7 被推翻**：系統字的 `tabular-nums` 對這批候選字體全部無效（Google Fonts 靜態字重實例不帶那個 feature），數字寬度實測不等寬，不是「夠不夠一致」的品味題，是「有沒有真的做到 tabular」的功能題。v2.7 改自架 Archivo 600，理由與範圍見「字體」章節與版本紀錄 |
| Cubic 11（點陣字，Gambit 同款） | 除上述理由外另有硬傷：點陣字只在設計尺寸的整數倍銳利（11 / 22 / 33 / 44px），Tally 的 14px 餐別熱量與 11.5px 營養素都對不上，且 14px 卡在 11 與 22 中間兩邊不合用——要採用就得推翻整個 1.25 字階。點陣字也沒有真正的粗體，撐不起 600 weight 的主數字 |
| 數字用 mono | 縱向對齊訴求本身成立（跟 v2.7 改 Archivo 動機相同），但 mono 額外帶來等寬字元的工程感，跟「溫暖平靜」的方向打架。Apple 自己也是 iOS 數字用 SF Pro 的 tabular figures，只有程式碼情境才用 SF Mono——Archivo 是這條「非 mono 但真 tabular」路線的落地 |
| 用一行字解釋剩餘額度是共用的（「接下來這兩餐還有 957 卡可以分」） | 主數字本身就是 957，時間軸下方就列著兩個待記錄餐次——共用是讀得出來的，不必再講一次。需要文字去解釋的介面，代表 mapping 本身弱 |
| 右上角寫成「已吃 903／目標 1860」兩行帶標籤 | 佔兩行、加兩個詞，換來的資訊等於 `903/1860` 加階層就能給。標籤是給第一次看的人，天天用的人只需要數字 |
| 分步 sheet（選餐別 → 選食物 → 填份量三屏推進） | 塞不進已記錄品項，且常吃一鍵變三步 |
| 食品庫用店家分兩層 | 23 筆裡 vendor 分散在 11 個店家、8 個只有一筆，群組數接近項目數，分層沒有換到查找效率 |
| 每列顯示份量 × 單價的即時小計 | 底部已有總計，記錄當下沒人算單品 |
| 刪除用底部動作選單 | 左滑露出再點刪除本來就是兩個動作，彈窗只是多包一層卻沒換到安全性 |
| 選取列鋪 `--accent-soft` 底色 | 三列連排形成大色塊，與 cream 背景打架；加減按鈕組出現本身已是最強訊號 |
| 把欄位名融進 placeholder | 一輸入就消失、對比度不足、螢幕閱讀器支援不一致，是 WCAG 3.3.2 常見失敗案例 |
| 今日頁每個餐別直接放常吃 chip | 四排 chip 約 144px，而 745px 基準下破表屏已溢出 96px，會毀掉一屏不捲 |

## 尚未設計

- 破表且仍有待記錄餐次時的主數字文案（「超出 229」對還沒吃晚餐的人資訊不足）
- 剩餘額度的分配比例規則（第一版不做，只顯示共用總額）
- 今日頁品項列要不要顯示店家：三個同名「雞胸餐盒」在今日頁看起來一模一樣（sheet 的清單已分兩行解決）

## 版本紀錄

- **v1 · 2026-07-27** — 走完設計流程 ⓪→➊→➋。骨架＝時間軸＋共用額度＋一屏不捲＋CTA 拇指區；色系＝Gambit cream + jade（配色樣張比過 5 欄，cream+jade 勝出）。
- **v1.1 · 2026-07-28** — 破表狀態細琢。斜紋改疊白細紋（原實色亮條在 cream 上太吵）；補上 v1 沒驗證的漏洞——3px 營養素條上斜紋會糊掉，改由破表加高至 6px 當主訊號。另補元件規則兩列：分頁列選中態、捲動範圍。
- **v1.2 · 2026-07-28** — 字體定案：**取消 `--font-mono`**，數字改用 `--font-body` ＋ `tabular-nums`，全站單一字體。比過六種自架 subset 方案後否決自架（理由見「已否決的做法」）。
- **v1.3 · 2026-07-28** — 走 apple-design 精煉。補 tracking／leading 分級；砍掉共用額度的解釋文字與右上角標籤，改 `903/1860` ＋前後階層；`:active` 補齊（原本只有 hover，手機按下去沒回饋）；1px 硬分隔線改捲動邊緣漸層；修正待記錄節點圓點錯位 12.1px（實測，非目測）。
- **v1.4 · 2026-07-28** — 三路獨立評審（a11y／UX／視覺）後的修補，共 60 條 findings。重點：**「一屏」的定義從 844 螢幕高改為 745 可視高**（原本整個版面建在真機不存在的高度上）；新增「語意與鍵盤」整節（原本規範完全沒有這層，是 `div` 當按鈕、無 landmark、清單不用 `ul` 的根因）；修正 v1.1 對分頁選中態的錯誤宣稱（`--raised` 對 `--card` 實算 1.14:1，撐不起辨識，改由 jade 文字＋accent 邊框承擔）；訊號色規則改為區分「狀態色」與「動作色」；補份量標記、虛實線端點規則、clamp／NaN、捨入規則。
- **v1.5 · 2026-07-28** — 今日頁實作落地（`index.html` / `app.css` / `app.js`），版型照 `mockup.html` 逐項對過。補七列元件規則：破表臨界（`>`）、主數字三數自洽的捨入順序、載入態、空狀態（決策為「什麼都不加」）、登入頁、第一版設定頁。登入頁與設定頁原列「尚未設計」，因規則已能唯一推導（token、CTA、字階全定死），未另走決策樣張。
- **v1.6 · 2026-07-28** — 修正禁止事項「不用任何外部請求」的表述。原句字面上把 Supabase 一起擋掉了（它是後端，一定要打），收窄為「不從第三方主機載入資源」。實質擋住的東西不變（webfont、CDN 函式庫），但不再被拿來當「不加依賴」的理由——那是獨立的判斷。連帶更正 `CLAUDE.md` 同一句。
- **v1.7 · 2026-07-28** — 「記一筆」流程定案（`sample-log-entry.html` 7 屏）：sheet 走同頁覆蓋層、可多選＋加減按鈕組取代熱量欄、已選段落固定在捲動區頂端且跨餐別保留；搜尋同時比對品名與店家，新增食物入口常駐清單末尾，表單改 floating label；刪除入口移回今日頁，左滑用 CSS `scroll-snap` 實作，品項本身仍是可點 `button` 保留鍵盤路徑；第一版納入日期切換（`input type="date"` ＋ 左箭頭，歷史日主數字改顯示攝取量）。熱量單位全站定案為「卡」。「記一筆」流程自「尚未設計」移除，改列設定頁編輯版範圍已定。
- **v1.8 · 2026-07-28** — 第一版功能全部落地：「記一筆」sheet、今日頁刪除、日期切換、設定頁編輯＋記體重。實作中修掉四件樣張沒答的事：floating label 的 52/18/13 會讓標籤與輸入文字重疊（改 60/26/8＋`line-height: 1`）；品項寬度是分數值（320.4px）導致刪除鈕沿右緣露出一條紅線（`margin-left: 1px` 推出裁切外）；底部確認列的 `.line` 與時間軸連接線的 `.line` 撞名被壓成直排（改 `.pick-line`）；在設定頁存檔會被丟回今日頁（頂欄與分頁改由 `renderHeader` 統一渲染、`state.tab` 記住分頁）。另補三條元件規則：設定頁的編輯與記體重、floating label 高度、設定頁第一版內容更新。
- **v1.9 · 2026-07-28** — 走完設計流程 ➍（四路並行：`hallmark` 快篩、`web-design-guidelines` 合規、`impeccable` 深評、`emil-design-eng` polish 審表），19 條 findings 全數處理。**兩個 P0 都是「看起來成功、其實沒有」類**：`db()` 沒有逾時，收訊差時 fetch 永遠不 reject，按鈕停在「加入中…」而使用者會以為記好了（補 `AbortSignal.timeout(8000)` ＋中文錯誤文案）；載入失敗後點設定會進到空殼分頁且回不去重試（補 `state.failed`，失敗期間留在提示畫面）。修掉兩條違反本檔硬約束的：品項列沒有 `:active`（全站唯一零回饋的可點元素）、floating label 過場動 `top`／`font-size` 兩個 layout 屬性（改 `translateY` ＋ `scale(.82)`，`.82 × --t-base` 正好是 `--t-xs`）。無障礙實算修正三處：未勾選圈 2.99:1、floating label 與 placeholder 2.92:1、輸入框邊框 1.37:1（新增 `--rule-field` token）。「觸控區 ≥44」與「加減組上限 130px」兩條規則原本互不相容，把鈕間距收成 0 後 44+42+44 剛好同時成立。另補：今日頁**同名品項才顯示店家**（三筆「雞胸餐盒」在今日頁本來完全一樣）、滑開刪除時品名用 `position: sticky` 釘在左緣（原本被推出視野，等於在看不到刪什麼的情況下按刪除）、常吃記住上次份量、歷史日的確認列不再寫「剩 N」。
- **v1.10 · 2026-07-28** — 使用者對 v1.9 的兩處裁決。**清單屏拿掉標題文字**（chip 選中態已經說了是哪一餐），關閉鈕併進 chip 列同一行——只拿掉文字省不到高度（那一行是 44px 關閉鈕撐的），要連鈕一起移才真的收回 52px；三張表單相反，保留標題。**三大營養素改選填、留空當 0**——原本判定「不改」是把場景套錯了：無糖飲料的蛋白質脂肪是**真的 0**，不是「查不到」，`not null` 的 schema 也給不出第三態，逼人打零沒有換到任何準確度。熱量維持必填。
- **v2.0 · 2026-07-29** — v2 UI 打磨定案（樣張 `sample-v2-today.html` v3／`sample-v2-login-icon.html` 第三～五輪，多輪使用者裁決）。**日期區**：砍原生 `input type="date"`，格式「週二 7/28」＋左右箭頭＋T1 安靜文字鈕「回今天」。**左滑刪除**：CSS scroll-snap 退場，改 `react-swipeable-list`（iOS 模式）＋ iOS 提醒事項風格圓形刪除鈕，真機 smoke test 為採用 gate。**分頁列**：pill 置中收窄（--r-pill、寬度收成內容自身、整條約 70px）。**登入頁**：極簡化（Meta splash 式）——小 mark＋問句當主角＋CTA 沉底；靈感來自使用者掃 FB/Threads/IG 啟動畫面。**Wordmark 終結**：全產品不出現，字體題（比過系統字/Space Grotesk/Outfit/Fraunces 四案）作廢，零 webfont 維持無例外。**App icon** 定案方向 3 幾何 T。動效四級階梯全站鋪開。
- **v2.1 · 2026-07-29** — iPhone 真機第二輪四點回報全處理。**左滑刪除換掉 `react-swipeable-list`，改 `motion` drag 手刻**（v2.0 就寫好的退路）：讀套件原始碼確認它拖曳中每一幀同時設 `transform` 與 trailing actions 的 `width`（`esm.js:615-627`），改 width 等於每幀觸發版面重排，這是「跟 iOS 有差」的實體來源，而且在套件核心設計裡、調參數解不掉。換掉後同時消掉四筆為了留著它而背的債：覆寫它三個內部 class 名、替它補裝未宣告的 `prop-types`、手刻鍵盤路徑（它沒有程式化開合 API）、`resetState` 的型別自己補。**狀態從三個並行 id 收斂成單一 `openId`**。**新增滑到底刪除與 5 秒復原窗**（兩者綁在一起，缺一不可）。**品項卡自己補 `border-radius`**——先前滑開時右緣是直角，因為圓角一直是外層 `overflow: clip` 裁出來的，卡片本身沒有。刪除改樂觀移除＋`AnimatePresence` 退場，其餘列用 `layout` 的 FLIP 滑上來（只動 transform／opacity，沒有動 height，守住「不動 layout 屬性」）。
- **v2.2 · 2026-07-30** — iPhone 真機第三輪的兩點回報。**刪除鈕的紅圓改畫在 `::before`（40px），門檻預告 `scale(1.18)` → `scale(1.1)`**：44px 圓在 44px 列裡放大 1.18＝51.9px，被 `overflow: clip` 切掉上下各 4px（實測 cutTop/cutBottom 各 4.0），視覺上是「圓角變大又被截斷」——兩個各自正確的修法（門檻預告、修水平溢位）撞在一起。**復原提示條改成右下角 44px 深色 pill**（`--ink` 底、undo 箭頭＋「復原」、整顆可點）：原本滿寬 353×62 的 `--raised` 條對 `--paper` 只有 1.14:1，太大塊又看不出來。
- **v2.3 · 2026-07-30** — 版面高度重排，走 `ui-design-flow`（⓪ 跳過＝沿用 v2.2 視覺語言；➊ 兩輪決策樣張 `sample-v3-frame.html`／`sample-v4-bottom.html`，使用者裁決）。起因是真機體感「日期區不太好」「底部擠又高」，實測底部兩段合計 138px＝可視高度的 18.5%。三項裁決：**① 底部併成一列**（分頁 pill 靠左、56px 圓形 CTA 靠右），時間軸 355→439px；**② 主 CTA 改純加號圓鈕、文案規則作廢**——使用者理由是這個 app 之後不只記飲食，「記一筆」這個文案本身會過期（樣張輪我推薦的是保留文字的膠囊版 B-a，被這個理由推翻，記在這裡免得未來把文字加回去）；**③ 日期組 `‹ 日期 ›` 置中、箭頭貼日期兩側、日期本身當 h1**，靜態頁名「日記」拿掉。連帶：復原 pill 改置中浮在底部列上方。三個候選的省空間量幾乎相同（+84～88px），所以這輪是純構圖裁決，不是效率裁決。
- **v2.4 · 2026-07-30** — v2.3 上機後的三處修正。**① 日期組包成一顆膠囊**（樣張 A，使用者裁決；起點是他觀察到 iOS 26 把裸圖示鈕包進容器）——裸 chevron 在 cream 上沒有可按的訊號，但**只借構圖不借材質**，理由與被否決的「兩顆箭頭各包一圓」寫在「日期切換」條。**② 「回今天」移到頂欄左端、拿掉前置 chevron**——貼右時與箭頭只隔 8.03px（長日期時 ~2.5px，破 Material 8dp 地板）；依據是 7 個產品的調查，結論是**否證式的**：沒有任何手機觸控 app 把文字型「今天」與方向箭頭小間距並排，主流是放對側。**③ 分頁選中態底色改 `--accent-soft`**——純圖示化之後 jade 與 muted 的亮度只差 1.02:1，狀態訊號實際只剩 1px 環在扛（fresh verifier 用 WCAG 公式實算抓到）。另有一條**有意識略過**：復原 pill 會蓋住時間軸最後一列 5 秒——補底部 padding 要吃掉 v2.3 省下空間的一半以上，而那一列還在、一捲就出來。
- **v2.5 · 2026-07-30** — iPhone 真機第四輪回報，四項 ＋ 一項效能。**① 日期膠囊視覺縮到 36px**（觸控維持 44px，手法同刪除鈕的 `::before`）——使用者說「比裸箭頭好按很多，但稍嫌搶戲」。**② 分頁鈕 44→56px 寬 ＋ 選中指示器改成會在兩顆之間滑過去的 `motion` layoutId FLIP**（`--dur-mid`，尊重 `prefers-reduced-motion`）。**③ 刪除的 undo 窗改成跨日期存活**：換日期不再結清（換分頁仍結清），`pendingDelete` 多記一個 `date`；復原只在同一天才插回畫面，不同天就只取消計時器（那筆本來就還沒真的刪）；`loadDay` 從「重撈前先結清」改成「**濾掉**待刪那筆」——結清會讓「切走再切回」變成莫名其妙真的被刪掉。**④ App icon 反轉配色 ＋ 方形滿版**（見「App icon」條）。**⑤ 日期切換加快取與預取**（見「日期切換的體感」條）。實作期另外揪出一個既有 bug：`commitDelete` 的 identity 跟著 `loadDay`→`currentDate` 連動，害 `pagehide` effect 的 cleanup 每次換日期都偷送一次真的 DELETE——用 ref 切斷依賴。
- **v2.6 · 2026-07-31** — 新增食物表單的店家欄位改用 Base UI `Autocomplete`（首次引入這個元件），取代純 text input：既有店家可搜可選，也能自由打新店家。撞到一個兩層 portal 的坑並修掉——下拉預設 portal 到 `document.body`，但 vaul 的 Drawer 底層是 Radix Dialog，開啟時 `body` 被設成 `pointer-events: none`，選單看得到點不到；改用 `Portal` 的 `container` prop 指到 `Drawer.Content` 解決。細節見「元件規則」新增的「店家欄位」條。
- **v2.7 · 2026-07-31** — 兩項使用者實機裁定。**① 數字換字體：Archivo 600（自架 woff2，`src/assets/`）**——看過並排樣張後裁定「只換數字，中文維持系統字體」。選 Archivo 不是好看，是硬指標：實測十個數字的寬度差，Archivo 僅 0.09px（天生 tabular figures），候選字體 Outfit 12.89px／Space Grotesk 8.88px／Manrope 9.68px／Sora 14.56px 全部落敗，且對它們套 `font-variant-numeric: tabular-nums` 完全無效（Google Fonts 靜態字重實例不帶那個 feature）——這也連帶推翻了 v1.2「`tabular-nums` 靠系統字體就夠」的假設，見上方「字體」章節。套用範圍是數字為主的顯示元素（`.gauge-num`／`.pair`／`.node-kcal`／`.item .kc`／`.item .qty`／`.food-row .kc`／`.qty-value`），**中文數字混排的元素不套**（`.date-title`、`.settings .kv dd`、`.pick-bar .sub`／`.remain` 這類「130 卡」「剩 1193」的字串留在系統字體，兩種字體同框在同一句話裡看起來破碎）。**② 日期字級調小**：`.date-title` 從 `--t-lg`（20px）/700 改 `--t-md`（16px）/600，理由見「日期切換」條。
- **v2.8 · 2026-07-31** — 店家 Autocomplete 兩處微調（使用者實機回報）。**排序明釘筆畫**：`localeCompare` 的 locale 從 `'zh-Hant'` 改成 `'zh-Hant-u-co-stroke'`——V8／WebKit 實測目前預設 collation 就是筆畫序（跟明釘結果逐字相同），但預設值不保證跨引擎/版本穩定，明釘讓排序規則寫進程式碼而不是隱含在瀏覽器行為裡。**未採用「依食物數量排序」**：`.foods` 裡 11 個店家 8 個只有一筆（見上方「已否決的做法」的分層理由），數量幾乎全部打平，排出來的順序不會比字母序更有意義。**收窄下拉頭尾空白**：`.vendor-popup` 拿掉自己的 `padding: var(--s-1) 0`，只留 `.vendor-item` 本身的 `var(--s-2) var(--s-3)`——原本兩層 padding 疊加，第一/最後一項前後多出一條沒有 hover 反應的死區，看起來像多餘留白。
- **v2.9 · 2026-07-31** — 修掉「某餐最後一筆刪除時退場動畫不會跑」的缺口（session-state 原本記成「已知未覆蓋」，實測後發現是真的壞了，不只是沒測到）。細節見「元件規則」的「今日頁品項刪除手勢」條。
- **v2.10 · 2026-08-01** — 額度預警提示定案（決策樣張 `sample-quota-warning.html` 核可後固化，樣張定稿後移除）。走過 Musk 視角審查（第一版「蛋白質達標／熱量脂肪碳水超標」四訊號混色方案被攔下——`DESIGN.md` 當初否決蛋白質達標視覺的理由是「六天資料從沒達標過」，這輪沒有新證據推翻它）後回到需求源頭重談，收斂成兩個各自成立、互不相關的機制：**逐筆超標預警**（食物清單，避免選了會超熱量/脂肪/碳水的東西，見「元件規則」新增條）＋**底部確認列加蛋白/脂/碳誠實數字**（回答「蛋白質吃夠了沒」，不靠顏色）。**完全不需要新增任何顏色狀態**，也不用碰「蛋白質不足不轉紅」這條既有規則——兩者原本以為互斥，拆開機制後才發現壓根不衝突。
- **v2.11 · 2026-08-01** — 真機（真實資料，不是 fixture）試用後兩處回修。**逐筆超標預警拿掉熱量重複**：`.kc` 數字本身跟下面的 delta 小字都在講熱量，使用者回報「看起來像同一件事講兩次」——改成 delta 只列脂肪／碳水，熱量交給 `.kc` 變色與底部確認列的 `(+N)` 講，螢幕閱讀器版（`formatOverAria`）不受影響、三項照唸。**底部確認列的顏色判定訂正**：v2.10 文字誤寫成「三個都不做顏色判定」，實作也照誤寫的規則做了——正確規則是今日頁三大營養素條那條既有規則的延伸（脂肪／碳水各自 >100% 轉 `--over`，只有蛋白質不轉紅），不是這裡另外發明一套「全部不判色」。兩處都是使用者拿真實資料試用才浮出來，demo 樣張的資料量太小、太乾淨，測不出這兩個問題。
- **v2.12 · 2026-08-01** — push 前 `precommit-review`（deep）抓到 6 個確認成立的問題，全修。
  (1) aria-label 在「只有熱量超標、脂肪碳水都沒有」時整段消失——v2.11 把 `formatOverDelta` 拿掉熱量後，`LogSheet.tsx` 判斷要不要接 `formatOverAria` 的閘門還是看視覺字串 `delta`，改成直接判 `formatOverAria(over)` 自己的回傳值。
  (2) 超標 0.1～0.4 會顯示「+0g脂」——`rowOverage` 判定粒度是 0.1，顯示粒度是整數，兩支 format 函式的顯示開關改成「捨到整數後還大於 0」而不是原始值大於 0。
  (3) `.pick-line`／`.macro-line` 補 `role="group"`——generic `<div>` 靠 `aria-label` 命名是 ARIA 禁止的模式，子元素又全部 `aria-hidden`，沒有 role 這兩段資訊螢幕閱讀器可能整段讀不到。
  (4) 逐筆預警的份量從固定 1 改成 `defaultQty`——見上方「逐筆超標預警」條。
  (5) `MealNode` 換日期時 key 併上 `date`——見「今日頁品項刪除手勢」條的 `renderTimeline` 說明，修掉「切到快取住、那餐剛好是空的日期」被誤判成「剛刪除」而誤放退場動畫的問題。
  (6) 零碎：`.pick-bar` 拿掉沒有 CSS 依賴的 `is-over` class；`pickTotals` 兩段串接的 `.map()` 合成一段。
- **v2.13 · 2026-08-02** — 真機第五輪回報，三處確認成立的問題全修＋一項字重補強（emil-design-eng 顧問後定案）。
  (1) 新增食物表單進場焦點跳到熱量，跳過店家——品名已由搜尋字串帶入，下一格該落焦的是店家（`LogSheet.tsx` 的 `useEffect`）。
  (2) 店家下拉選單上方有一段死留白、下方沒有——`Autocomplete.Empty` 不管有沒有結果都留在 DOM 裡，有結果時沒有子節點但 padding 還在，`.vendor-empty:empty { padding: 0 }` 收掉。
  (3) 「加入」後今日頁的高亮閃爍「看起來很廉價」——峰值 `.9` opacity、accelerate 曲線撐 `1.2s` 是舊式「整塊變綠又暴斃」的成功提示樣式，改成峰值 `.55`、`--ease`、`700ms`。
  (4) 字重層次補強：自架 Archivo 原本只裝 600 一個字重，次要數字（`.item .kc` 這類沒特別設字重、理論上該繼承 400 的元素）因為瀏覽器沒有更輕的字形可選，實際全部跟主數字一樣粗——加一份 400 的 `@font-face`，兩邊 CSS 規則不動，字形一到位自然分流，見「字體」章節。
  (5) 日期標題套用 Archivo——見「字體」章節「日期標題是例外」條，使用者拿放大截圖比對後裁定套用。
  (6) qty stepper 加減鈕與中間數字框邊界重疊（真機回報「小數才會重疊」，其實整數也會、只是打整數多半用 +/− 不會聚焦輸入框才沒發現）：`.qty-value:focus-visible` 原本 `outline-offset: 1px` 是正值外擴，三顆 `gap:0` 緊貼時外框直接蓋到左右鄰居的邊框上；改成 `-2px` 內縮（同手法見 `.item-delete:focus-visible`）。
  (7) App icon「黑底白T」：repo 內與 GitHub Pages 線上實測的 PNG 都已是 v2.5 綠底反轉配色，判斷是 iOS 對 `apple-touch-icon` 的快取太黏、單純移除重加不夠——`index.html` 的圖示連結加 `?v=2` 版本參數強制瀏覽器視為新資源重新抓取，使用者需重新「加入主畫面」一次驗證。
- **v2.18 · 2026-08-02** — 今日頁編輯份量的長按觸發，真機回報「先看到刪除鈕（左滑 reveal）
  閃一下，才掉編輯 sheet」，100% 重現、手指完全不動、閃出來的狀態會維持到 sheet 蓋上去。
  根因是 iOS 的 click 事件合成常以 pointerdown 當下的目標為準（不像桌機在放開當下重新
  hit-test），所以即使 sheet 的 scrim 已經蓋住這一列，補來的 click 仍可能命中底下的
  `.item-content`，觸發 `onToggle()`。`blockClickUntil` 原本在長按計時器**觸發當下**
  就設成 `now+150ms`，但真實使用者是「看到 sheet 才放手」，反應時間常態超過 150ms，
  窗口早就過期。改成跟拖曳同一套 v3 手法：計時器觸發時先設 `Infinity`（放手前一律擋），
  放手當下才收斂成 `now+CLICK_GRACE_MS`，grace window 永遠從真正放手那一刻算起。
  **這個修法自己又引入一個迴歸，被同一輪 push 前 review 抓到**：sheet 一開整片 scrim
  蓋住這一列，button 沒有 `setPointerCapture`，放手時 `pointerup` 會落在 scrim 上，
  `.item-content` 自己的 `onPointerUp` 根本不會觸發——原本只有 `onPointerUp` 會把
  `blockClickUntil` 收斂回有限值，`onPointerCancel`／`onPointerLeave` 只清計時器不碰
  這個值，導致長按觸發後如果收尾走 cancel/leave（桌機滑鼠常態如此），`blockClickUntil`
  會卡在 `Infinity` 永遠出不來，那一列的滑動 reveal 直到觸發一次 drag 前都打不開。
  修法是讓 `onPointerCancel`／`onPointerLeave` 也走同一支收尾函式。
- **v2.17 · 2026-08-02** — v2.16 的背景色調也被真機核可打回：使用者看過後認為「背景色塊跟整體風格不搭」——這個 app 一路是「不鋪底色、靠明度分層」的極簡路線（見「選取態不鋪底色」條），背景色塊即使 alpha 壓得很低，還是引入了一種這個 app 原本沒有的視覺語言。改用者提議的第三個方向：**完全不做視覺降權**，`.over-quota` 只剩排序與測試用的語意標記，靠既有的排序位置＋`.kc.over`／`.kc-delta` 紅字表達「這筆超標」，不新增任何顏色。連帶刪掉一條測試——原本驗「淡化列按下要有可辨的按壓回饋」的測試，因為它驗的行為（over-quota 列有自己的按壓樣式）已經不存在，不是留著改斷言、是整條移除。三個版本（opacity → 背景色調 → 無視覺處理）都留在這裡當紀錄，不要重蹈覆轍再試一次視覺降權。
- **v2.16 · 2026-08-02** — v2.15 淡化機制的 opacity 改成背景色調，**廢除 opacity 降權**。真機用真實資料核可 v2.15 時，使用者回報某一餐幾乎全部超標、全部套同一個 `.9` opacity，肉眼完全看不出哪些被降權——為了讓「店家」子行過 WCAG AA 對比度而一路把 opacity 推到 `.9`，代價正是把淡化效果推到幾乎消失，數字再怎麼調都是死路。改成 `.food-item.over-quota .food-row { background: rgba(154,67,48,.07) }`（按下 `.14`），文字完全不動，對比度問題直接消失，不用再算。使用者當場在三個「不動文字透明度」的方向裡選了背景上色（另兩個是左側色條、完全不做視覺降權只排序），細節與理由見「元件規則」該條。
- **v2.15 · 2026-08-02** — 記一筆 sheet 加「以今天剩餘的空間，我還能吃什麼」的排序＋淡化。三方案（A 硬篩選／B 軟性排序／C 門檻式）的本機 demo 比過後使用者裁決 B：不隱藏任何品項，只把符合今日額度的排前面、不符合的降視覺優先度，結構上不可能清空清單。全部食物／搜尋結果依「加這一筆還吃得下嗎」排序（判斷基準用 `eaten`，刻意跟逐列變色用的 `combined` 不同源——見「元件規則」新增條的完整理由）；常吃只淡化不重排，維持 recency 順序。新增 CSS `.food-item.over-quota .food-row`（`src/app.css`，opacity 定案 `.9`，過程見下方三處回修），複用既有的 `rowOverage`／逐筆超標預警機制判定，零新增顏色。**push 前 fresh-context verifier 兩輪複驗，共修三處**：① 淡化 opacity 原訂 `.55`，`--ink` 疊上去對 `--card` 只有 3.49:1，掉到 WCAG AA 文字門檻（4.5:1）以下——第一輪改成 `.64`（`--ink` 實測 4.53:1）；② `.food-item.over-quota .food-row`（0-3-0）的特異度蓋過既有 `.food-row:active`（0-2-0），淡化列在手機上按下去完全沒有按壓回饋（`:active` 是唯一訊號，沒有 hover）——補一條 `:active` 覆寫；③ 第二輪 verifier 逐一量了淡化列裡每個文字元素才發現 `.64` 只顧到 `.nm` 品名，`.sub` 店家（`--ink-muted`）只有 2.75:1——店家是「區分同名品項的唯一資訊」，這個退讓站不住（不像 `.kc-delta` 是輔助資訊，退讓可以接受）。**opacity 改定案 `.9`，`:active` 改 `.65`**，四個文字元素（`.nm`／`.sub`／`.kc`／`.kc-delta`）全部過 AA，實測數字見「元件規則」該條。
- **v2.14 · 2026-08-02** — 新增今日頁品項編輯份量：長按品項開一個自建 sheet 改 `qty`，不必刪除整筆重加。細節見「元件規則」新增的「今日頁品項編輯份量」條。**push 前 fresh-context verifier 抓到一個必修的資料不同步缺陷**：長按計時器沒有隨 `SwipeRow` unmount 清除，該列已經從畫面消失（換日期／刪除退場動畫跑完）後計時器仍會觸發，對一筆看不到的品項開 sheet 並真的寫進 DB，畫面卻停在切走前的快照——補上 unmount 清理修掉。另外訂正「元件規則」對取消門檻的描述：實際生效的是 framer 拖曳辨識的 ~3px 內部門檻，自己補的 10px `pointermove` 只在 framer 完全不介入的原生垂直捲動時才派得上用場，原文字誤把兩者寫成同一層次。**同一輪複驗又抓到同病灶的第二個入口（F2）**：編輯 sheet 開著時（沒有 focus trap，跟 `Settings.tsx` 既有的自建 sheet 一樣，這次範圍不修），背景仍可操作換日期讓正在編輯的那一列離開畫面，此時存入一樣會送出寫錯對象的 PATCH。改成**通用修法**而非逐路徑補丁：`App.tsx` 的 `handleUpdateIntakeQty` 在真正送出 PATCH 前，統一檢查這個 id 是否還在目前的 `rows` 裡，不在就靜靜跳過（不送、不報錯），F1 的計時器路徑與 F2 的 sheet-已開路徑因此共用同一道防線，未來任何讓該列離開視圖的操作路徑都會被同一個檢查點擋住。
- **樣張現況** — 過程樣張（`design-sample-v2` / `palette-options` / `over-stripe-options` / `font-options`）決策已全數固化進本檔，2026-07-28 移除；v2 打磨樣張（`sample-v2-today.html` / `sample-v2-login-icon.html`）同理，2026-07-29 移除，內含的三段 base64 字型 subset 隨 wordmark 題作廢一併消失。最後兩份（今日頁 `mockup.html`、「記一筆」流程 `sample-log-entry.html`）**2026-07-31 移除**——它們畫的是 07-28 vanilla HTML 時代的 v1.5／v1.7 樣貌，專案 07-29 轉 React + Tailwind 棧後又走過 v2.0→v2.6（底部併成一列、日期膠囊、分頁滑動指示器、CTA 改純圖示），照著做會做出三個版本前的畫面。**設計規則自此以本檔的元件規則表為唯一真相來源**，不再有「實作以某份樣張為準」的旁路。上方版本紀錄裡 v1.5、v1.7 對這兩份的引用保留不動，那是歷史紀錄不是現況宣告。**v2.7 補記**：`sample-fonts.html`／`sample-fonts2.html` 是 Archivo 定案前的並排比較樣張，決策已固化進「字體」章節與 v2.7 版本紀錄，同樣不列入現存樣張、留給下次大清一併處理。
