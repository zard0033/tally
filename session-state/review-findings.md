# 今日頁樣張 — 三路評審 findings 清單

2026-07-28。三個獨立 agent（a11y／code、UX／產品、視覺）對 `mockup.html` ＋ `DESIGN.md` 的評審，共 60 條。
本檔只留**尚未處理**的；已修的直接進了 `DESIGN.md` v1.4 與 `mockup.html`，不在此重複。

實作時逐條對。每完成一條就從這裡刪掉。

---

## A. 必須在接上 Supabase 時一起做（會直接壞掉）

- [ ] **寫入失敗回饋**。純前端直打 Supabase 無本地佇列，而記錄發生的地點（美食街、地下室）正好收訊最差。按完「記一筆」三秒關掉 app，不會發現沒存進去。不必做離線佇列（YAGNI），但失敗要當場看得見。
- [ ] **記錄流程的返回成本**。若「記一筆」是獨立頁面，每次返回＝整頁重載＋重打 Supabase，「三秒瞄一眼」變「三秒等載入」。決定同頁覆蓋層還是獨立頁；獨立頁的話今日頁要有本地快取。

## B. 沒設計過的狀態（畫面存在但沒人畫過）

- [ ] **破表 ＋ 仍有待記錄餐次**（每週數次）。真實的破表通常發生在一天還沒過完時（午餐吃爆、晚餐還沒吃），紅色量尺與虛線待記錄行同框。這是使用者最需要答案的時刻（「已經爆了，晚餐還要不要吃」），而此時主數字的「超出 229」對還沒吃晚餐的人資訊不足——文案可能要換。**版面已驗證可渲染，未解的是文案。**
- [ ] **食品庫為空／搜尋無結果**。屬「記一筆」流程，但與 UID 分裂相關：新帳號時 foods 是空的，「新增食物」是唯一出路。
- [ ] **0 kcal 食物**（無糖飲料、代糖）。記錄後品項顯示 `0`、小計不變、量尺不動，跟「沒存到」無法區分。

## C. 第一版功能缺口

- [ ] **「刪除當天某筆」零入口**。`spec.md` 第一版白紙黑字的功能，但品項列不可點、無滑動、無長按。第一版又沒有編輯（第二版才有），刪除是唯一糾錯手段——沒入口＝一錯永久錯。
- [ ] **已記錄餐次無法追加**。早餐記完想補一杯咖啡，「早餐 293」整塊不可點，只能按 CTA 從頭再選一次餐別。同一條時間軸上兩種節點一可點一不可點，**而不可點的那種恰好是需要修改的那種**。
      → 建議：讓餐別標題列也成為入口，一次解掉這條和上一條。
- [ ] **記錄成功沒有確認訊號**。回到今日頁只有數字變了；記一個 70 kcal 的茶葉蛋，主數字 957→887 在三秒的一瞥裡不見得注意得到。建議新品項進場給一次極輕提示（只用 opacity/transform）。

## D. 邊界與規則（實作時會撞到）

- [ ] **長食物名截斷門檻**。實測 390px 下品名約 285px 才截斷，現有最長品名 `雞胸餐盒(糙米+萵苣)|波奇(Poki)` 佔 199px，安全。但使用者的命名法是 `品名|店家`（23 筆裡 8 筆帶 `|`），而**店家正是區分三個「雞胸餐盒」的唯一資訊**——截斷會先砍掉它。份量標記已加上，餘裕被吃掉了一些，實作後重測。
- [ ] **`prefers-contrast: more` / `forced-colors`** 完全沒處理。優先度低，但 `--ink-faint`、`--rule` 這些逼近門檻的用色沒有安全網。

## E. 待裁決

- [x] ~~第一版要不要做歷史檢視~~ → 2026-07-28 決定**不做**（照 `spec.md` 第二版範圍），日期箭頭已從樣張移除。
      連帶待第二版處理：歷史日的主數字文案時態（「還能吃」對過去的日子無意義）、在歷史日按「記一筆」記到哪一天、右箭頭在今天要停用、標題可點回今天、跨午夜的日期判定（23:50 吃宵夜 00:10 才記）。

---

## 已處理（2026-07-28 前端骨架，僅供追溯）

NaN 防護（profile／weight 缺漏走降級畫面）／session 過期與空資料分開（401 清 session 回登入頁，
200 + `[]` 才是真的空）／載入態（主數字停在破折號，時間軸明說「載入中」）／
`eaten == target` 定為 `>` 才算破表／空狀態（決策：不加最小可見態、不填空白，見 `DESIGN.md` v1.5）／
單一營養素破表＋總熱量沒破（已渲染驗證）／中間餐次空著（虛實線查 DOM 驗證，非目測）。

## 已處理（v1.4，僅供追溯，不必再看）

版面高度改 745px 可視高／語意與鍵盤整節／`--raised` 錯誤宣稱修正／訊號色規則區分狀態色與動作色／
營養素標籤 3px 錯位／數對 aria-label／份量標記／虛實線端點規則／clamp 與 NaN／捨入規則／
`-webkit-mask-image` 前綴／`safe-area-inset-bottom`／`hover` 包 `@media (hover: hover)`／
`div` 改 `button`／`ul`/`li`／landmark ＋ `h1`／`lang`／chevron 與空心圓點的對比／
`.item` 硬編 3px／CTA 圖示 2.2 的例外入規範／`--ink` 對比數字更正 13.6:1／`spec.md` 的「環圈」殘留。
